Empty at init — no business logic yet (.ai/plans/init-source.plan.md §8, "No business logic during init").
The scheduler domain's modules (e.g. `booking/{domain,application,infrastructure,presentation}`)
go here, following directives/folder_structure_sop.md's Hexagonal layout.
