{"timestamp": "2026-08-17T04:30:00+00:00", "type": "architecture", "title": "Phase 0 scaffold built directly from init-source.plan.md, no code before the plan", "detail": "Executed plan §12 Phase 0 in full: root workspace, packages/scheduling-core's skeleton (frozen public surface as throwing stubs, value objects implemented and tested), apps/web's Next.js shell + Prisma schema + seed, the ported/trimmed AI-workflow apparatus (directives rewritten for Vitest+Next.js+SQLite instead of Jest+NestJS+Postgres, since scenario 01's versions assumed a stack this scenario deliberately doesn't use), the full docs/ADR set, and CI. Chose to leave scheduling-core's five pipeline stages as throwing stubs rather than empty shells or fake implementations — a stub that throws 'not implemented' is honest; a fake implementation that returns plausible-looking empty data would silently pass the wrong tests later. Phase 1 (the algorithm itself) is next.", "context": "plan §12 Phase 0"}
{"timestamp": "2026-08-17T13:10:00+00:00", "type": "architecture", "title": "init plan §7.2's calibration rule does not produce its own stated answer of N=18 — it produces N=15", "detail": "Plan §7.2 specifies suggestTransactionsPerStaff as a binary search for N where floorStaffHours(N) ~= targetUtilisation (0.8) x sum(maxWeeklyHours) (368) = 294.4, then asserts this 'yields N ~= 18 - 272 floor staff-hours against a 294-hour target'. Re-deriving floorStaffHours over N=8..40 from the committed CSV: N=15 gives 296 (off by 1.6), N=18 gives 272 (off by 22.4). N=15 wins under nearest, largest-N-with-floor-above, and smallest-N-with-floor-below. N=18 is the 5th-best candidate; the plan's own sentence states the 272-vs-294 mismatch without noticing it. This is load-bearing because 18 is @default(18) in prisma/schema.prisma, is in the shipped migration, and §7.8's entire seed demo is computed at it. Every other figure in §7.1/§7.2/§7.8 reproduced EXACTLY (112 cells, 3058 txns, all 7 rows x 4 columns of the calibration table, 46/34/38 seats, U_min=32 via sum(ceil(0.6*cap/8)) per staff rather than 0.6*368/8=27.6) - so the defect is isolated to this one derived claim, not to the measurement. Resolution options and the recommendation (ship 18, have suggest honestly return 15, explain the gap in ADR-0003) are in .ai/plans/phase-1-algorithm.plan.md D1/§2.0. Lesson: a plan that computes a table correctly can still state a wrong conclusion from it; re-derive the CONCLUSION, not just spot-check the table.", "context": ".ai/plans/init-source.plan.md §7.2, §7.8; .ai/plans/phase-1-algorithm.plan.md §1 D1; apps/web/prisma/schema.prisma"}
{"timestamp": "2026-08-17T13:12:00+00:00", "type": "architecture", "title": "An executed plan is annotated in the successor plan, never edited in place", "detail": "Phase 0's agent proposed 'sua lai cau do trong plan §9/assumption 15' after finding that plan §9's 'no .env required (SQLite path has a default)' is technically false - Prisma's env(\"DATABASE_URL\") has no default syntax, so a missing .env fails prisma generate with P1012. The diagnosis and the fix (commit apps/web/.env, negate it with !apps/web/.env in .gitignore) are both correct and verified in place. The proposed REMEDY is not: CLAUDE.md's Citation Protocol says 'Never retouch a plan after execution - leave the wrong prediction in and annotate it, that contradiction IS the evidence'. So init-source.plan.md §9 keeps its wrong sentence and phase-1-algorithm.plan.md §1 D3 carries the annotation. Corollary found while checking: the fix stopped at the .env file, and the stale 'no .env required' claim still survives in 4 places including the .env.example written by the same change (apps/web/.env.example:1, docs/09_running_it.md:11 and :29, readme.md:72, RUN.md:10). Reword to what is true - 'no .env to create; apps/web/.env ships committed' - rather than deleting the claim.", "context": "CLAUDE.md Citation Protocol; .ai/plans/init-source.plan.md §9; apps/web/.env.example; docs/09_running_it.md"}
{"timestamp": "2026-08-17T13:20:00+00:00", "type": "architecture", "title": "D1 resolved by the user: ship N=18 as the default, let suggestTransactionsPerStaff honestly return 15", "detail": "Chosen against two alternatives that were put on the table with their costs: (B) move the default to 15 - rejected because a new migration plus a re-derived §7.8 table would drop the demo's slack from 8 seats to 2 (32<37<44<46), a knife-edge outcome that could flip on an implementation detail; (C) retune targetUtilisation to ~0.74 so the formula outputs 18 - rejected as a constant reverse-engineered from its own answer. Option A costs no schema/migration/seed change, keeps the demo ordering comfortable (32<34<38<46), and turns the discrepancy into evidence rather than hiding it: ADR-0003 must state 'the data-driven suggestion is 15; we ship 18' with the reason being that N=15 commits 296 of 368 contracted hours to the floor alone, leaving nothing for the peak top-up or the rebalance pass to work with. Implication for Phase 1: suggestTransactionsPerStaff's tests assert 15 for the seed team, and that is NOT a bug against the @default(18) in schema.prisma - the two numbers are deliberately different and the README states both.", "context": ".ai/plans/phase-1-algorithm.plan.md §1 D1 and §2.0; docs/adr/0003-demand-to-headcount-model.md; apps/web/prisma/schema.prisma"}
{"timestamp":"2026-08-17T15:40:41.705Z","type":"architecture","title":"buildSchedulingInput extracted to application/shared/ -- both AutoScheduleHandler and AddAssignmentHandler must shape Prisma rows into SchedulingInput identically","detail":"generateRoster (auto-schedule) and validateRoster (manual roster edit) both need the exact same SchedulingInput built from the same Schedule/StaffMember/Shift/DemandCell rows -- scheduling-core's own index.ts docstring warns about this class of drift for the FeasibilityGate itself ('one implementation of the rules, two callers ... a second copy is how the two paths drift'). The same risk exists one layer up, in the Prisma-row-to-SchedulingInput mapping, so it was factored into application/shared/build-scheduling-input.ts rather than duplicated when AddAssignmentHandler was added -- AutoScheduleHandler was refactored to call it too. Any FUTURE handler that needs a SchedulingInput (e.g. the coverage view) should call this helper, not re-derive the DemandGrid Map-building loop inline.","context":"apps/scheduler-api/src/modules/scheduling/application/shared/build-scheduling-input.ts"}
{"timestamp":"2026-08-17T16:21:28.877Z","type":"architecture","title":"Coverage view recomputes Diagnostics LIVE from the persisted roster -- docs/04_data_model.md originally specified the opposite (read ScheduleRun.diagnostics), corrected once manual roster editing existed","detail":"docs/04_data_model.md said the coverage view should read ScheduleRun.diagnostics (a stored snapshot from the last auto-schedule run) specifically to avoid recomputing something that might disagree with what the manager saw at generation time. That reasoning predates manual roster editing (AddAssignmentHandler/RemoveAssignmentHandler) -- once an assignment can be added/removed after the last run, the stored snapshot goes stale the instant the manager edits, and GetSummaryHandler had already made the opposite call for the summary report for the identical reason ('re-derives it from the persisted roster every time, so it stays correct after a manual edit'). GetCoverageHandler was built to match that existing precedent: it rebuilds RosterState by replaying detail.assignments through a fresh FeasibilityGate (same public surface generateRoster/validateRoster use -- computeRequiredStaff/computeShiftRequirements/FeasibilityGate/RosterState/buildDiagnostics, all already exported from scheduling-core's index.ts), then calls buildDiagnostics. Verified live: DELETE on one assignment dropped that hour's `scheduled` count on the very next GET .../coverage call, no auto-schedule re-run needed. docs/04's comment corrected in place with a dated note (not silently rewritten) rather than left contradicting the code -- any future read endpoint over the roster (not just coverage/summary) should default to this same live-recompute rule unless there's a specific reason to read a stored snapshot instead.","context":"apps/scheduler-api/src/modules/scheduling/application/queries/get-coverage/get-coverage.handler.ts"}
{"timestamp":"2026-08-17T17:22:59.528Z","type":"architecture","title":"docs/adr/README.md's ADR index and its closing 'nothing is ported verbatim' line went stale the moment ADR-0005 was ported in Phase B, and nobody had reconciled the index since","detail":"ADR-0005 (transaction-retry-boundary, ported from ../service-appointment-scheduler in Phase B) was added to docs/adr/ as a file but never added to docs/adr/README.md's index table -- and that file's own closing sentence ('Numbering starts at 0001 because nothing is ported verbatim... into this scenario's own ADRs') became factually false at the exact moment ADR-0005 landed, since it explicitly IS ported verbatim. This is the same class of gap as the naming_conventions.md/domain_modeling.md/zod_validation.md debt (directives/README.md, Phase F) -- a new pattern or file gets built without its OWN index/description being updated in the same task, even though the After-Task protocol says to. Fixed in Phase F. Lesson for future work: when porting or adding anything with its own index file (an ADR, a directive, a memory category), update the index in the SAME commit as the addition, not as a later doc-polish pass -- this is the second time in this repo's history that gap has been caught only in a dedicated reconciliation phase instead of when it was introduced.","context":"docs/adr/README.md, docs/adr/0005-transaction-retry-boundary.md"}
{"timestamp":"2026-08-17T18:08:57.374Z","type":"architecture","title":"Created ../_templates/ at the collection root -- canonical copies of the 3 directives that are genuinely stack-agnostic in mechanism (memory_sop.md, qa_standard.md, cqrs_pattern.md), to copy into future scenarios instead of retyping from memory","detail":"User's suggestion, in response to finding real drift (cqrs_pattern.md missing, qa_standard.md pointing at a deleted directory) between what should have been near-identical files across the two scenarios in this collection. ../_templates/directives/ holds {{PLACEHOLDER}}-marked versions of exactly these three files -- NOT naming_conventions.md/domain_modeling.md/zod_validation.md/testing_standard.md, which are correctly scenario-specific in content even when their section headings rhyme; templating those would either produce something too generic to be useful or reintroduce the same copy-paste drift this folder exists to prevent. ../_templates/README.md documents the usage protocol explicitly: copy in, fill every {{PLACEHOLDER}}, add a provenance note, and -- the rule most likely to be forgotten -- port a REAL fix made in a scenario's copy back to the template in the same task, or the template regresses to worse-than-current and starts propagating the exact staleness it exists to prevent. This template folder itself is unproven until a third scenario actually uses it; treat it as a hypothesis about what reduces drift, not a guarantee, until that happens.","context":"../_templates/ (collection root, outside any single scenario)"}
{"timestamp":"2026-08-17T18:50:18.251Z","type":"architecture","title":"FIVE common directives were missing from this repo, not one: cqrs_pattern, folder_structure_sop, logging_standard, database_standard, observability_monitoring -- all ported in, plus idempotency_strategy kept as ⏸-not-needed rather than absent","detail":"User corrected the model behind the earlier 'not ported, doesn't apply' calls: these are COMMON documents (Cortex -> service-appointment-scheduler -> here). Surplus is not wrong -- a scenario may not use a section, but it must not delete it, because a rule deleted for being unused is a rule nobody knows to re-add when it becomes relevant. Verified against the real code before porting, not assumed: prom-client/collectDefaultMetrics/`register` (observability), LogContext+createLogger+nestjs-pino across 8+ files (logging), 35 instances of @map/@default(uuid())/deletedAt (database), and eslint.config.mjs's own layer-boundary rules (folder_structure). All four genuinely apply NOW. The root cause of all five absences: init-source.plan.md §5 judged them inapplicable against the pre-reversal Next.js+SQLite stack, and NOTHING re-checked that judgment after the backend-architecture reversal reinstated NestJS/Postgres/CQRS/Docker -- the same stale-premise class as the CLAUDE.md/AGENTS.md finding in the previous turn. Ported files keep scenario 01's exact section numbering so in-code `§N` citations stay valid; sections that don't apply here are marked ⏸ with a trigger instead of cut (observability §1/§3/§4 = Prometheus/Grafana deferred; idempotency = not needed by construction; folder_structure's idempotency//observability//value-objects//mappers slots = reserved-but-empty). Two places where copying verbatim would have been DANGEROUS and were adapted with the divergence stated inline: database_standard §5 (scenario 01 is Prisma 7 where `url` in the datasource block is an error; this repo pins Prisma 5 where it is REQUIRED -- copying sc01's text would have told an agent to break the schema) and §2's uuid(7) vs uuid() (v7 needs Prisma 6+).","context":"directives/ (5 files ported), ../_templates/directives/"}
{"timestamp":"2026-08-17T19:06:05.236Z","type":"architecture","title":"Audited Cortex (distributed-social-platform) as the upstream root of the shared directives — it is CLEAN; every documentation defect found this session was introduced downstream in the port, not inherited","detail":"Checks run against ../../distributed-social-platform: (1) every `directives/*.md` path referenced anywhere in its directives/docs/.ai resolves to a real file — the single apparent miss, `event_driven_sop.md`, appears only in two plans as a PROPOSAL ('cân nhắc thêm' = consider adding), correctly phrased as future work rather than a broken link; (2) its `directives/README.md` index lists all 18 directives, none unlisted; (3) every `docs/**.md` cited from a directive exists. So Cortex has better cross-reference hygiene than scenario 02 had before this session — the referenced-but-missing bug class was created in the Cortex→sc01→sc02 port chain, not propagated from the source. IMPORTANT context discovered: **Cortex's directives are written in Vietnamese** (16/19 contain Vietnamese prose); scenario 01 translated them to English when porting. This means a naive `diff` of section headings between Cortex and a scenario shows large fake 'dropped section' counts that are really translation artifacts (`## 📜 Luật Lệ (Rules)` → `## 📜 Rules`). The genuinely-dropped groups (naming_conventions' Guard/Caller/gRPC-Client; domain_modeling's entities-vs-aggregates; folder_structure's core-api-vs-auth-service comparison) were each dropped WITH a documented reason and a re-add trigger in scenario 01's own header — the discipline the user is asking for was already being followed at that boundary. Practical rule: when comparing a scenario's directive against Cortex, compare RULES not headings, because the language differs.","context":"../../distributed-social-platform/directives/ (read-only audit)"}
{"timestamp": "2026-08-18T06:23:47.000Z", "type": "architecture", "title": "apps/web's mutation screens refresh via router.refresh() after every write, never local optimistic state -- one pattern across all six new screens (staff/demand/shifts/roster/summary/coverage)", "detail": "Every new screen's mutation handler (addStaff, updateShift, addAssignment, autoSchedule, ...) does: setPending(true) -> await the api-client call -> on success call next/navigation's router.refresh() (re-runs the Server Component's data fetch for the current route) -> on failure setError() for a Banner. No screen holds its own copy of server data that diverges from what getSchedule()/getSummary()/getCoverage() would return on a fresh load -- refresh() is the single re-fetch path, matching frontend_standard.md's rule that data fetching belongs in the server component, not client state. Chosen over optimistic UI updates because this is a single-user, single-process local app (assumption 14) where an extra round-trip is imperceptible, and because optimistic state would need its own reconciliation logic for the FeasibilityGate's 422s (an optimistic add can be REJECTED by the gate) -- refresh-after-confirm sidesteps that whole class of bug for a marginal latency cost. The one exception: RosterManager's drag-and-drop keeps the dragged assignment in the DOM until the destination add is confirmed successful, specifically so a gate-rejected drop leaves the original assignment in place rather than optimistically vanishing it.", "context": "apps/web/src/components/{staff,shift,demand,roster}-manager.tsx, summary-view.tsx, coverage-view.tsx"}
{"timestamp": "2026-08-18T08:43:45+07:00", "type": "architecture", "title": "GET /schedules/:id/suggested-n now 422s via a new InsufficientCalibrationDataError when staff, shifts, or demand cells are empty, instead of silently returning a near-meaningless N", "detail": "Traced the full call path (suggest-n.handler.ts -> buildSchedulingInput -> suggestTransactionsPerStaff in packages/scheduling-core) and confirmed every empty-input edge case (no staff, no shifts, no demand) was ALREADY crash-safe by construction -- computeRequiredStaff loops over an empty DemandGrid Map as a no-op, computeShiftRequirements pins closed-hour/zero-length-shift to floor=target=0 rather than NaN, and Prisma findMany returns [] never null/undefined. So this guard is NOT a defensive-against-a-crash fix; it exists because the algorithm silently degrades to bestN=1 (a number that looks legitimate but reflects zeroed-out math, not a real calibration) when one of the three inputs is missing. Chose to fail loudly (422, code INSUFFICIENT_CALIBRATION_DATA, details listing exactly which of {staff, shifts, demand} is missing) over returning 1 with no signal, because the UI's 'Suggest from data' affordance (docs/01 assumption 1) implies the number is trustworthy -- returning 1 without a flag would look like real data-driven advice. Deliberately scoped to suggested-n ONLY, not auto-schedule: generateRoster must never throw on feasible-but-bad input (CLAUDE.md hard rule) -- an empty roster + honest diagnostics is the correct auto-schedule behavior for missing data, whereas suggested-n has no diagnostics channel to carry that caveat, so 422 is the only way to signal it.", "context": "apps/scheduler-api/src/common/errors/scheduling.error.ts, apps/scheduler-api/src/modules/scheduling/application/queries/suggest-n/suggest-n.handler.ts"}
{"timestamp": "2026-08-18T09:15:00+07:00", "type": "architecture", "title": "Split assignment/feasibility-gate.ts (155 lines) into eligibility-brand.ts + roster-state.ts + feasibility-gate.ts -- file organization only, still exactly 2 mutable classes", "detail": "User found the file hard to read: the Eligibility Symbol-branding trick, the RosterState class (7 methods), and the FeasibilityGate class (H1-H3) were all in one file, so reading a rule meant scrolling past unrelated branding-mechanics comments first. Considered converting scheduling-core to OOP generally, but rejected: directives/domain_modeling.md SS1 pins FeasibilityGate/RosterState as the ONLY mutable classes allowed in the package, specifically because index.prop-spec.ts property-based-proves H1-H3 never break -- that proof only holds because there is exactly one gate every assignment must pass through; adding more mutable classes risks a second mutation path and breaks the proof premise. Counted the package: ~36 total functions/methods but only 7 are on generateRoster's public pipeline path (computeRequiredStaff, computeShiftRequirements, FeasibilityGate.eligible, fairnessPass, coveragePass, rebalance, buildDiagnostics) -- the rest are private per-file helpers a reader only needs when debugging that specific stage, so neither OOP nor further file-splitting reduces function count; it only changed which functions sit next to which. Split: eligibility-brand.ts (the Symbol trick + Eligibility/EligibilityData types, read once), roster-state.ts (RosterState, the mutable accumulator), feasibility-gate.ts (trimmed to ~60 lines of pure H1-H3 rule logic, re-exports the other two so no external import site needed to change). Zero logic change -- 80/80 tests pass including the property-based flagship spec; typecheck and lint clean (pre-existing unrelated parserOptions lint errors on *.spec.ts files confirmed present on master via git stash before this change).", "context": "packages/scheduling-core/src/assignment/feasibility-gate.ts, packages/scheduling-core/src/assignment/eligibility-brand.ts, packages/scheduling-core/src/assignment/roster-state.ts, directives/domain_modeling.md"}
{"timestamp": "2026-08-18T10:10:00+07:00", "type": "architecture", "title": "Bundled the always-together (gate, state) pair into one RosterContext value object, threaded through fairnessPass/coveragePass/rebalance/buildDiagnostics instead of two positional params", "detail": "User asked to reconsider directives/domain_modeling.md S1 (\"no second mutable class\") because manually threading gate+state through every stage function felt error-prone -- proposed wrapping the pipeline in an OOP class with this.state. Clarified the directive only forbids a SECOND MUTABLE class, not classes in general, so a thin orchestrator wrapping the existing FeasibilityGate/RosterState pair would not technically violate it -- but rejected the full-OOP path anyway: it would not have fixed the actual complaint (method-call ORDER is just as unenforced as function-call order; OOP does not add sequencing safety without a state machine), while costing a rewrite of 7 spec files (assigner.spec.ts, rebalancer.spec.ts, diagnostics.spec.ts, demand-model.spec.ts, shift-requirements.spec.ts, feasibility-gate.spec.ts, index.spec.ts all construct gate/state as plain values and call pure functions directly -- moving stages into instance methods breaks that per-stage unit-testability) and changing index.ts's \"frozen at init\" public surface (plan S2.3). Chose instead: RosterContext, a plain readonly interface { gate, state } -- NOT a class, so it does not add a mutable-class count and stays within S1's letter and spirit -- because gate and state are ALWAYS the same pair from the same SchedulingInput at every call site; bundling them removes the exact risk flagged earlier (a caller passing a gate from one input alongside a state from another) and halves stage-function arity (4-5 positional args down to 2-3) without touching per-function testability: a test still just writes an object literal, no constructor. generateRoster rebuilds the RosterContext (not mutates it in place) after rebalance(), the one stage that returns a NEW RosterState rather than mutating the existing one. Found and fixed a call site outside the package that would have silently broken: apps/scheduler-api's get-coverage.handler.ts calls buildDiagnostics directly with the old 5-arg signature -- caught by npm run typecheck --workspace=apps/scheduler-api failing after rebuilding scheduling-core's dist/ (apps/scheduler-api imports the built package, not src/, so typecheck must run AFTER npm run build --workspace=@scheduler/scheduling-core to see a scheduling-core signature change).", "context": "packages/scheduling-core/src/assignment/feasibility-gate.ts, packages/scheduling-core/src/assignment/assigner.ts, packages/scheduling-core/src/assignment/rebalancer.ts, packages/scheduling-core/src/reporting/diagnostics.ts, packages/scheduling-core/src/index.ts, apps/scheduler-api/src/modules/scheduling/application/queries/get-coverage/get-coverage.handler.ts"}
{"timestamp": "2026-08-18T22:20:00+07:00", "type": "architecture", "title": "Role minimums (stretch goal) enforced as a seat-filling pass (rolePass), not a sixth FeasibilityGate constraint -- new ADR-0006", "detail": "The obvious first instinct given ADR-0001s H1-H4 precedent was a fifth/sixth gate case answering \"may this candidate take this seat\". Rejected: a role minimum answers a structurally different question (\"is this seat legal yet, regardless of who\"), and forcing it into FeasibilityGate.eligible breaks three things at once -- rebalancer.tss withoutAssignment throws on a previously-valid assignment that becomes infeasible on replay (exactly what happens removing a seats only supervisor), permutation-determinism (index.prop-spec.ts assertion 3b) breaks because a per-seat rule evaluated per-candidate depends on whos already committed which depends on iteration order, and Violation has no staffId-less shape for it. Chose instead: rolePass, a new stage in assigner.ts run FIRST in generateRoster (most-constrained-first -- a role minimum is the narrowest filter of any seat-filling rule, so filling it first makes a reported shortfall reflect genuine capacity lack not fill-order accident). FeasibilityGate/validateRoster are completely unchanged (D5) -- no new ReasonCode, no new gate case. Diagnostics.roleShortfalls (new required field) is the sole reporting surface, never thrown. rebalancer.ts gained roleCoverageDidNotFall alongside the existing coverageDidNotFall (which only compares per-seat COUNTS, so a swap can preserve headcount while destroying role coverage -- moving a shifts only supervisor to a non-required shift changes zero seat counts). Full reasoning: docs/adr/0006-role-requirements-as-seat-requirements.md.", "context": "packages/scheduling-core/src/assignment/assigner.ts, packages/scheduling-core/src/assignment/rebalancer.ts, packages/scheduling-core/src/reporting/diagnostics.ts, docs/adr/0006-role-requirements-as-seat-requirements.md"}
{"timestamp":"2026-08-18T17:10:00.206Z","type":"architecture","title":"User correction: the backend architecture is baseline engineering, not scope surplus — README now states scope explicitly instead of apologising for the stack","detail":"I had been framing the NestJS/CQRS/Postgres backend defensively, repeatedly hedging it against the brief's 'do not gold-plate' line (section 9) and treating the public-repo/link questions as risks. The user corrected both premises directly: the company authorised a public repo, features only need to meet the stated requirements, and — the substantive point — architectural design is a baseline engineering competency, not decoration to be justified away. The fix they asked for was not to remove anything but to make the boundary explicit: a Scope section in the README naming what the brief required (all of sections 2.1-2.6 plus 5, with the verified numbers), and separately what went past it. The important distinction the section draws, and the one to preserve in future edits: the ARCHITECTURE is not the beyond-scope part — the brief leaves the stack open, so a domain layer and a real database are a decision rather than a requirement, but structuring seven features so the next developer can change them safely is ordinary engineering, and it costs the reviewer exactly one extra command. What IS genuinely beyond scope is enumerated honestly and separately: all five optional stretch goals (section 8), property-based testing, and the committed decision trail. Lesson for future sessions on this repo: do not re-litigate the stack against 'do not gold-plate', and do not describe the architecture as surplus in any document — state the scope boundary plainly and let the feature surface (which matches section 2 exactly, no more) carry the argument.","context":"readme.md (## Scope), readme.vi.md (## Phạm vi)"}
{"timestamp":"2026-08-19T03:56:38.799Z","type":"architecture","title":"npm audit: fixed the one reachable production vulnerability (@fastify/static, unused code path), deliberately left next/vitest majors unpatched with the exploitability reasoning recorded","detail":"9 findings (3 moderate, 5 high, 1 critical), and every single one required a semver-MAJOR bump -- `npm audit fix` (no --force) applied nothing and just listed 'breaking change' for all nine, confirming there was no free lunch here. Rather than running --force blindly right before submission, checked actual reachability per finding: (1) @fastify/static (direct dep of apps/scheduler-api, path-traversal/auth-bypass advisories) is registered ONLY inside @nestjs/platform-fastify's useStaticAssets() (grep confirmed: `node_modules/@nestjs/platform-fastify/adapters/fastify-adapter.js:311`, `loadPackage('@fastify/static', ...)` inside that one method) -- and grep across apps/scheduler-api/src found zero calls to useStaticAssets() anywhere, matching an existing gotcha entry that this package is a pure boot-time peer requirement with no real static-file route ever registered. The vulnerable code path is present in node_modules but never reachable at runtime. Bumped it anyway (9.1.3 -> 10.x via `npm install @fastify/static@latest --save-exact` scoped to apps/scheduler-api) since it's a real production dependency and the bump is zero-risk when nothing calls its API -- verified by full rebuild + boot (`GET /health` -> `database: ok`) + all 255 tests green, not just installed-and-assumed-fine. (2) The remaining 5 chain entirely off `next` (postcss, sharp -- both bundled INTO next's own toolchain) and `vitest` (esbuild, vite, vite-node, @vitest/mocker). Checked actual attack surface before accepting the risk rather than just deferring: grep confirmed apps/web never imports next/image (sharp's libvips CVEs need next/image processing untrusted uploads -- unreachable, no image feature exists in this app at all) and no package.json script runs a raw `vite`/`vite dev` server anywhere (grep confirmed) -- vite/esbuild/vite-node/@vitest/mocker are pulled in purely as vitest's internal test-run bundler, never exposed as an HTTP dev server to a network, so their advisories (both about a MALICIOUS WEBSITE reaching a live vite dev server) don't apply to this repo's usage pattern. next itself (15 -> 16) is the one real framework-major bump this leaves on the table -- deliberately not attempted this close to submission: App Router/build-output changes across a Next major have historically been non-trivial, and there is no time budget left to fully re-verify all seven UI screens against a new major before the deadline. Documented rather than silently ignored, per this repo's own rule that a deferred decision needs a stated trigger.","context":"apps/scheduler-api/package.json (@fastify/static), package-lock.json. Deferred: next (15->16), vitest (2->4) and their transitive chains."}
{"timestamp":"2026-08-19T06:52:47.553Z","type":"architecture","title":"Coverage/Summary now declare when the roster predates the current parameters, with an explicit Recalculate action — chosen over blanking the figures, and the asymmetry that caused the confusion is the reason","detail":"User: editing a parameter silently changed the derived screens, which felt out of their control, and asked for the values to reset to 'no value' with a recalculate button. Diagnosed the real mechanism first, because the two screens do NOT react the same way: coverage's `required` column is recomputed from `demand / N` on every read (GetCoverageHandler calls computeRequiredStaff with the CURRENT parameters), so editing N moves those numbers instantly even though nothing was re-planned; the summary's staff-hours derive only from the persisted roster and do not move at all. Same edit, two different visible reactions, no explanation on screen — that inconsistency, not the recomputation itself, is what read as 'not in control'. Implemented `rosterStatus(schedule, latestRun)` in apps/web/src/lib/staleness.ts (pure, 7 unit tests, following the repo convention that non-trivial UI logic lives in lib/ and is tested there) comparing the schedule's current parameters against the ones auto-schedule stored in ScheduleRun.parameters -- no API change needed, latestRun already flowed to the client. Returns NEVER_RUN / STALE-with-the-changed-parameter-names / CURRENT. A shared `RosterFreshness` banner on both screens names the changed parameters and offers one action: re-run auto-schedule. DELIBERATE DEVIATION from the literal request, flagged to the user rather than done silently: the figures are NOT blanked. Blanking would mean a manager who merely typed a new N can no longer see the coverage of the roster they actually have, and the summary's staff-hours were never affected by that edit in the first place — so the banner says plainly that nothing has been re-planned and that the numbers still describe the current roster. One real trap in the comparison: `maxStaffPerHour` is ABSENT in the stored run parameters but `null` on the schedule row, so a raw !== reported a change on every single read and would have made the warning permanent noise; compared through `?? null` instead, with a test pinning that exact case.","context":"apps/web/src/lib/staleness.ts (+spec), apps/web/src/components/roster-freshness.tsx, app/s/[id]/{coverage,summary}/page.tsx"}
{"timestamp":"2026-08-19T14:45:00+07:00","type":"architecture","title":"rosterStatus extended from the 4 Schedule-row parameters to staff/shift/demand/role inputs, via 4 timestamp columns + a shared touchSchedule helper, not a ScheduleRun snapshot","detail":"User: rosterStatus (previous session) only compared the 4 Schedule-row parameters (N, minStaffWhenOpen, maxStaffPerHour, minUtilisationTarget) against ScheduleRun.parameters -- adding/removing staff, editing shifts, re-importing demand, or changing roles never touched the Schedule row, so the STALE banner stayed silent on exactly the inputs the user named (staff/role/demand/shift). Considered storing a full input snapshot on ScheduleRun mirroring the parameters column, rejected: staff/shifts/demand/roles are collections not scalars, so a snapshot would mean diffing arrays of rows, not a !== per field. Chose 4 nullable {staff,shifts,demand,roles}UpdatedAt columns on Schedule instead, each stamped now() by a shared touchSchedule(tx, scheduleId, category) helper (infrastructure/repositories/touch-schedule.util.ts) called from every repository that writes the corresponding table (StaffMember, StaffUnavailability, Shift, DemandCell, Role, StaffRole, ShiftRoleRequirement), inside the SAME transaction as the write. rosterStatus compares each stamp against ScheduleRun.generatedAt, null treated as unchanged (never an epoch date). Availability grouped under Staff, role/seat-requirement edits grouped under Roles, rather than becoming a 5th/6th category. Required a live-Postgres migration (add_schedule_input_freshness_timestamps) -- prisma generate EPERM-failed on the first attempt because a stale `npm run start` prod stack still had the client DLL open; killed with explicit user permission, then generate succeeded. Verified live in a real browser, not just unit tests: added staff after a run -> Coverage/Summary named Staff; added a shift + a role after a run without re-running -> Summary named Shifts, Roles together; Recalculate cleared the banner both times.","context":"apps/scheduler-api/prisma/schema.prisma (Schedule.*UpdatedAt), infrastructure/repositories/touch-schedule.util.ts (new), 7 Prisma*Repository files, domain/entities/schedule.entity.ts, prisma-scheduling.query-repository.ts, apps/web/src/lib/staleness.ts (+spec), apps/web/src/lib/api-client.ts"}
{"timestamp":"2026-08-19T14:58:00+07:00","type":"gotchas","title":"RosterFreshness banner was wired onto Coverage and Summary but not Roster -- the third screen the same staleness applies to, caught by the user noticing the inconsistency","detail":"Roster is where the persisted assignments actually live (the grid), and it already has its own primary Auto-schedule button + a Last-run timestamp, so it was easy to treat the freshness banner as belonging only to the two DERIVED read views. That reasoning misses that Roster's grid is just as capable of predating a later staff/shift/demand/role/parameter edit as Coverage's numbers or Summary's totals -- the grid does not re-render itself when the underlying inputs change, it is exactly as stale as the roster row it reads. Fixed by adding <RosterFreshness> to apps/web/src/app/s/[id]/roster/page.tsx (same rosterStatus(schedule, latestRun) call the other two pages already make) and generalizing the banner's copy, which had screen-specific wording baked in ('These figures describe...see real coverage' -- literally said 'coverage' unconditionally, would have been wrong on Roster and arguably on Summary too) to copy that reads correctly on all three ('This roster was generated before you changed X' / 'what's shown below still describes the roster you currently have'). Lesson: when a cross-cutting concern (staleness, permissions, loading states) is desugared into a shared component, audit EVERY screen the underlying condition applies to before wiring it into just the ones the immediate bug report mentioned -- 2 of 3 is an easy count to stop at without noticing the third.","context":"apps/web/src/app/s/[id]/roster/page.tsx, apps/web/src/components/roster-freshness.tsx"}
{"timestamp":"2026-08-19T17:10:00+07:00","type":"architecture","title":"Diagnostics.roleCapacity added (a per-role structuralVerdict) after a code-review request found the sole-role-holder fairness bottleneck is a genuine staffing gap, not a fixable algorithm bug -- rebalance's role-coverage guard deliberately left unchanged","detail":"User asked for a critical review of generateRoster's real gaps. Constructed a real repro (dist/index.js, not theory): 1 role, 1 holder X (max 40h), 1 daily 8h shift needing that role x7 days -> X pinned to 40h/100%, a second staff member Y stuck at 16h/40% (belowTarget), roleShortfalls on days 6-7, and rebalance provably CANNOT fix it -- every move of X's early-week shifts to Y is blocked by rebalancer.ts's roleCoverageDidNotFall guard (moving X off a seat only X can legally fill drops that seat's role-count). Initially framed this as an algorithmic unfairness bug (#1) and floated loosening the role-coverage guard as the fix. Re-investigated before touching code: swapping roleCoverageDidNotFall for a softer 'allow if fairness gain outweighs role-coverage loss' rule would let rebalance UNDO role coverage rolePass correctly achieved -- risks silently regressing a constraint ADR-0006 says must never silently regress (roleShortfalls is supposed to be the ONLY place a shortfall surfaces, never a side-effect of an unrelated fairness pass). Tested whether multi-holder cases are actually unfair too (2 holders, 6 required role-days, asymmetric max hours) -- the existing lowest-utilisation-first tie-break in rolePass/pickCandidate already produces the mathematically optimal split (3-3, or proportional to capacity) with ZERO code change needed. Concluded the single-holder case isn't a bug: it's the correct, honest surfacing of a real staffing shortage (not enough Supervisor-hours contracted for the week) -- rebalance refusing to fix it BY BREAKING ROLE COVERAGE is the correct behavior, not a limitation. The genuinely fixable problem was narrower: `roleShortfalls` and `staff[].belowTarget` read as two disconnected facts with no stated cause, which is what made this look like a bug in the first place. Fix: `Diagnostics.roleCapacity` (new field, mirrors the existing team-wide `structural` verdict but scoped to one role) -- `{roleId, requiredRoleHours, contractedRoleHours}`, always reported (not only when short), computed by reusing roleShortfalls' own closed-day skip so the two numbers can't drift apart. UI renders a banner ONLY when requiredRoleHours > contractedRoleHours, worded to state the gap is NOT fixable by rescheduling. Shared `RoleDiagnosticsBanners` component (new) renders both role banners identically on Schedule, Roster, and Coverage -- factored out during this change since 3 near-identical inline blocks were about to exist. 6 new scheduling-core unit tests (empty/always-reported/genuine-gap/closed-day-skip/unrelated-role-excluded/multi-role-sorted), 103 scheduling-core tests total (was 97). Verified against the LIVE api on the user's own test schedule, not just unit tests: added a real Supervisor role-requirement to an existing shift, ran auto-schedule for real -> API returned exactly {requiredRoleHours:56, contractedRoleHours:40} matching the synthetic repro, banner rendered correctly on Coverage ('Supervisor needs 56.0h... no amount of rescheduling can close that gap'), same banner appeared on Roster on page load.","context":"packages/scheduling-core/src/model/types.ts (RoleCapacity), packages/scheduling-core/src/reporting/diagnostics.ts (roleCapacityVerdict), apps/web/src/components/role-diagnostics-banners.tsx (new), apps/web/src/components/schedule-manager.tsx, apps/web/src/components/coverage-view.tsx, apps/web/src/lib/role-copy.ts (describeRoleCapacityGap), docs/06_api_contracts.md, docs/adr/0006-role-requirements-as-seat-requirements.md (unchanged, decision re-affirmed not reversed)"}
{"timestamp":"2026-08-19T17:12:00+07:00","type":"architecture","title":"Roster tab now fetches live coverage diagnostics too -- removing a role-holder's assignment on the Roster tab shows the resulting roleShortfall on THAT SAME tab immediately, not only on the next visit to Coverage","detail":"User-approved fix (#3 of the review): remove-assignment.handler.ts's own comment already admitted a removal 'CAN degrade role coverage' but is never gated -- the shortfall only ever surfaced on GetCoverageHandler's live recompute, meaning the exact moment of the risky action (clicking x on a shift's only Supervisor) was the most SILENT moment in the whole flow. roster/page.tsx now also calls getCoverage(id) (same live-recompute-from-persisted-roster call Coverage itself makes, docs/04_data_model.md's dated note) and renders the same RoleDiagnosticsBanners used on Schedule/Coverage, above the grid. router.refresh() after every add/remove already re-runs this server component, so the banner updates on the very next render with no new client-side wiring needed. Verified live: removed the schedule's only Supervisor from Monday's Morning shift on the Roster tab -- 'Mon Morning has no Supervisor' appeared in the SAME banner list on the SAME tab, no navigation.","context":"apps/web/src/app/s/[id]/roster/page.tsx"}
{"timestamp":"2026-08-19T15:15:00+07:00","type":"architecture","title":"Roster tab split into Schedule (parameters + Auto-schedule + diagnostics) and Roster (grid + manual add/remove/drag-drop + CSV) -- user-directed, Auto-schedule stays with the parameters not the grid","detail":"User: the old single Roster tab conflated editing tunables, triggering generation, and hand-tuning the resulting grid -- asked to split into a Schedule tab (params) and a Roster tab (info/manual editing), then asked directly where Auto-schedule should live since it wasn't obvious from the split description. Asked via AskUserQuestion rather than guessing on a structural UX call with two defensible answers; user chose Schedule (grouped with the parameters it consumes -- 'adjust N, click run' as one motion) over Roster (grouped with the grid it produces). Extracted schedule-manager.tsx (params form, Suggest from data, Auto-schedule button, diagnostics banners, Last-run timestamp) out of the former roster-manager.tsx, which now only keeps the day x shift grid, manual add/remove/drag-drop, and CSV export. Both new screens (schedule/page.tsx, trimmed roster/page.tsx) render the same RosterFreshness banner via rosterStatus -- neither derived screen should be the one place staleness silently doesn't apply. New nav slug 'schedule' added between Shifts and Roster in schedule-tabs.tsx. Real pre-existing bug found and fixed in the same pass, not left in place: roster/page.tsx had ALWAYS called getSuggestedN unconditionally at page load, and that route 422s (INSUFFICIENT_CALIBRATION_DATA) the instant a schedule has no demand imported yet -- an unhandled throw in a Server Component crashes the WHOLE page with 'Application error', not just the one caption that needed the number. Reproduced live (schedule with staff but zero demand cells -> Roster tab 500'd) before fixing. Fixed with a page-level try/catch (getSuggestedNSafe) that treats ONLY that one error code as 'no suggestion available yet' (suggestedN: null, rendered as an 'import demand first' hint) and rethrows everything else -- verified the same schedule now loads cleanly on both Schedule and Roster tabs post-fix.","context":"apps/web/src/components/schedule-manager.tsx (new), apps/web/src/components/roster-manager.tsx (trimmed), apps/web/src/app/s/[id]/schedule/page.tsx (new), apps/web/src/app/s/[id]/roster/page.tsx, apps/web/src/components/nav/schedule-tabs.tsx, docs/05_ui_guidelines.md"}
{"timestamp":"2026-08-19T15:35:00+07:00","type":"gotchas","title":"\"Suggest from data\" button reported as broken (\"click, nothing changes\") -- verified NOT a functional bug, it was a UX-feedback gap plus a genuine misunderstanding the user's own framing already resolved","detail":"User: typed a different N into the parameter field, clicked Suggest from data, saw no visible change, asked if it was broken -- but also stated their own expectation correctly: 'one input set should yield exactly one N.' That is precisely why nothing visibly changed: the suggested N is a pure function of staff/shifts/demand ONLY (suggestTransactionsPerStaffHour, scheduling-core), completely independent of whatever is currently typed into the N field or even the persisted schedule.transactionsPerStaffHour -- so re-clicking with unchanged underlying data deterministically returns the identical number, by design (ADR-0003's whole point: current and suggested are allowed to diverge, comparison not auto-apply). Verified live: read_network_requests confirmed the button DOES fire GET .../suggested-n and gets 200 every click; the number just doesn't move. The real, narrower defect: the button gave literally NO visual feedback distinguishing 'did nothing' from 'refetched and confirmed the same answer' -- pendingAction was a single shared boolean covering Save/Suggest/Auto-schedule, so the pending flash (if any) was indistinguishable and easy to miss on an all-local near-instant request. Fixed with a discriminated pendingAction ('save'|'suggest'|'auto'|null) giving each button its own busy label, plus a 2-second 'checkmark Recalculated from current staff/shifts/demand' confirmation after every successful click regardless of whether the number changed, and reworded the caption to state explicitly that the suggestion ignores the N field above. A real debugging trap hit verifying the fix: get_page_text / a single javascript_exec snapshot taken even 300ms after the click MISSED the flash entirely on a local dev server (round-trip + 2s window still not reliably caught by poll-based checks) -- had to attach a MutationObserver logging every text mutation to actually observe the Checking… -> flash -> reverted sequence. Lesson for verifying any transient/timed UI state on a fast local backend: don't poll-and-snapshot, observe.","context":"apps/web/src/components/schedule-manager.tsx, packages/scheduling-core suggestTransactionsPerStaffHour, docs/adr/0003-demand-to-headcount-model.md"}
{"timestamp":"2026-08-19T15:45:00+07:00","type":"conventions","title":"\"Suggest from data\" button removed entirely -- user's follow-up after the pending-feedback fix: if the number never depends on anything the button re-sends, the button is pointless overhead, just show what page-load already fetched","detail":"Immediately after fixing the previous session's complaint (button gave no visible feedback), the user reframed the actual ask: since suggested N is fetched once at page load and is a pure function of staff/shifts/demand -- never of the button click itself -- re-fetching on click added no information. Removed the button, `refreshSuggestion`, `justSuggested`, and the local `suggested` state entirely; the Schedule page now renders `suggestedN` (the prop from the one server-side GET in schedule/page.tsx) directly, no client re-fetch path at all. Same turn, second piece of feedback: the explanatory caption was long and pale (`text-xs text-slate-500`) and the user said plainly 'ngan nhung ro hon dai ma mo khong ai doc' (short-and-clear beats long-and-unread) -- cut ~40 words down to one `text-sm` line: 'Suggested N: {n} - using {current}', dropped the auto-apply caveat prose entirely (it lives in ADR-0003 / docs/01, not repeated on-screen). General lesson worth carrying to any future info panel: when a value is deterministic and fetched once, don't dress it as an action (a button) just because an earlier iteration had one -- a static line is the more honest affordance, and a long caption nobody reads is worse than a short one that's actually seen.","context":"apps/web/src/components/schedule-manager.tsx, docs/05_ui_guidelines.md"}
