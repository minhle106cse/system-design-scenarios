{"timestamp": "2026-08-18T07:00:08.000Z", "type": "errors", "title": "api-client.ts request() sent Content-Type: application/json on a BODYLESS call (autoSchedule's POST, every DELETE) -> Fastify 400s with 'Body cannot be empty when content-type is set to application/json'", "detail": "Fix: only set Content-Type when init.body !== undefined and is not FormData (`const hasBody = init?.body !== undefined`). Symptom: clicking Auto-schedule in the browser threw that exact string as a banner.", "context": "apps/web/src/lib/api-client.ts request()"}
{"timestamp": "2026-08-18T07:00:08.000Z", "type": "errors", "title": "api-client.ts request() always parsed the response as JSON, including on 204 No Content (every removeX DELETE) -> the failed parse was read as body?.success being falsy and thrown as ApiError even though res.ok was true", "detail": "Fix: short-circuit on `res.status === 204` before attempting res.json(), returning undefined without parsing. Symptom: clicking Remove on a staff/shift/roster-assignment row banner-errored 'Request failed with status 204' despite the DELETE having actually succeeded server-side.", "context": "apps/web/src/lib/api-client.ts request()"}
{"timestamp":"2026-08-19T00:31:23.383Z","type":"errors","title":"Coverage grid label read \"Required staff vs. scheduled staff\" while the cell rendered `${scheduled}/${required}` — the two were inverted, so a manager would read every understaffed hour as overstaffed","detail":"Found during the final pre-submission UI pass, once port 3000 was free and the app could be driven in its SHIPPED configuration. coverage-view.tsx line 63 described the grid as 'Required staff vs. scheduled staff, per hour' but line 92 rendered `${cell.scheduled}/${cell.required}`. Verified against real data rather than by reading: Mon 09:00 showed '2/3' where the roster had 2 people on Mon Morning and demand was 39 transactions at N=18, i.e. ceil(39/18)=3 required — so the display is scheduled-first and the label named required first. A non-technical manager following the label reads 2/3 as 'need 2, scheduled 3, comfortably overstaffed' when the truth is the exact opposite, and the cell's red UNDERSTAFFED tint then contradicts their own reading of the numbers. Directly on the brief's graded 'is the UI clear enough for a non-technical manager' criterion. Fixed by rewriting the caption to state the order explicitly ('Each cell reads scheduled / required — the staff booked for that hour over the number the demand calls for') rather than by flipping the render, since the colour badge and every earlier verification session already assume scheduled-first. Lesson: a caption and the value it captions are two independent strings that no test compares — cross-check rendered NUMBERS against the source's template literal, not the caption's prose, and use a cell where the two values differ (a 4/4 cell would have hidden this completely).","context":"apps/web/src/components/coverage-view.tsx"}
{"timestamp":"2026-08-19T04:32:19.666Z","type":"errors","title":".gitignore's `coverage/` (meant for Jest/Vitest reports) silently swallowed apps/web/src/app/s/[id]/coverage/ — the Coverage SCREEN, a brief §8 stretch goal, was never committed and the pushed repo had 6 of 7 UI screens","detail":"Caught on the final pre-submission pass, and only because that pass ran the app from a FRESH CLONE with `npm run start` (production build) instead of `npm run dev` in the working tree. Symptom chain: /s/:id/coverage returned 404 while the API returned 200 for the same schedule id; the other five tabs all returned 200; .next/server/app/s/[id]/ contained demand, roster, shifts, staff, summary and no coverage directory; `git ls-files apps/web/src/app/s/` listed six page.tsx files, not seven. Root cause: `git check-ignore -v` pointed at .gitignore line 9, `coverage/` — a build-output pattern intended for test coverage reports (jest's coverageDirectory is ../coverage, vitest defaults to coverage/) that matches ANY directory named coverage at ANY depth, including a UI route directory under src/. Every previous verification missed it because they all ran against the local working tree where the file exists; the two earlier fresh-clone tests exercised the API and /roster //summary but never /coverage specifically. Fixed with a scoped negation (`!**/src/**/coverage/`) rather than by renaming the route or narrowing the ignore to `/coverage/`, and verified BOTH directions with git check-ignore: apps/coverage/index.html, apps/web/coverage/lcov.info, packages/scheduling-core/coverage/report.json and coverage/foo.txt all still ignored, while the route file is now trackable. Then swept every .ts/.tsx/.css under apps/*/src and packages/*/src through git check-ignore to confirm coverage was the only casualty (it was). Lessons: (1) a build-output ignore pattern must never be able to match anything under src/ — anchor it or negate src explicitly; (2) `git status` shows nothing for an ignored file, so a missing source file is invisible in the working tree and only appears in a clone; (3) verify a UI from a fresh clone of the PUSHED commit, exercising every route, not from the directory you built it in.","context":".gitignore (line 9 coverage/), apps/web/src/app/s/[id]/coverage/page.tsx"}
{"timestamp":"2026-08-19T06:33:42.607Z","type":"errors","title":"Removing a staff member permanently 500-ed the coverage endpoint: soft-delete left their assignments dangling, and FeasibilityGate.eligible THROWS on a staffId missing from SchedulingInput.staff","detail":"User reported coverage breaking right after removing staff. Reproduced against the live API and traced it: StaffMember is soft-deleted (deletedAt), so `listByScheduleId` stops returning them, but `RemoveStaffHandler` only called `staff.softDelete` -- their Assignment rows survived untouched. `GetCoverageHandler` rebuilds RosterState by replaying every persisted assignment through `gate.eligible(a.staffId, ...)`, and that method deliberately THROWS on an unknown staffId ('a caller bug, not a feasibility case -- scheduling-core trusts its input completely'). So one removal poisoned the schedule permanently: every later coverage read was a 500, and nothing ever cleaned the rows up. Summary survived because `summarise` skips unknown references instead of throwing, which is why the symptom looked like 'coverage is broken' rather than 'the data is broken'. Fixed in three places, deliberately: (1) `RemoveStaffHandler` now calls a new `assignments.deleteByStaffId` BEFORE the soft delete, (2) `RemoveShiftHandler` got the matching `deleteByShiftId` -- a dangling SHIFT reference never threw (coverage already skipped unresolvable shifts) but silently kept counting toward that person's booked hours, a quieter wrong answer, (3) `GetCoverageHandler` now skips assignments whose staffId is not in `input.staff`, because a READ endpoint must survive data a write path should never have left behind -- and without it every schedule already corrupted by the old bug would have stayed broken after the fix. Verified against the actually-corrupted schedule in the dev DB (500 -> 200) and by re-running the original scenario end to end (delete staff with 5 assignments -> coverage 200, summary 200, 0 rows left behind). Lesson: when a domain function documents that it throws on invalid input because it 'trusts its input', every caller that feeds it PERSISTED data owns that contract -- soft delete quietly breaks it, since the row survives the delete but disappears from the query the caller builds its input from.","context":"apps/scheduler-api/src/modules/scheduling: application/commands/remove-staff, remove-shift, application/queries/get-coverage, domain/repositories/assignment.repository.ts, infrastructure/repositories/prisma-assignment.repository.ts"}
{"timestamp": "2026-08-20T04:15:00.000Z", "type": "errors", "title": "Drag-and-drop on the roster grid was broken for every staff member near their weekly cap: it issued add-then-remove, so H1 validated a MOVE as if it were an ADD", "detail": "Found by an end-to-end review that ran the app rather than reading it, and confirmed with curl against the live API: POST /roster/assignments for a staff member at 24/24h returned 422 WOULD_EXCEED_MAX_HOURS. RosterManager.handleDrop called addAssignment(destination) then removeAssignment(source), ordered add-first on the reasonable-sounding grounds that a rejected add must leave the original seat in place. But AddAssignmentHandler replays the EXISTING roster plus the candidate through validateRoster, and the existing roster still contains the source assignment -- so the gate sees the person's weekly hours going up by a whole shift instead of staying flat, and H1 refuses. Anyone whose remaining slack is smaller than one shift could never be moved. That is not a corner case: the golden snapshot puts 4 of 12 seeded staff at exactly 100% utilisation, and the dev database's 8-staff schedule had all 8 there, so the brief's own first stretch goal failed on the demo data. Invisible to the whole test suite because both endpoints were individually correct -- the bug lived in the COMPOSITION, and nothing tested the composition. Fixed with PATCH /schedules/:id/roster/assignments/:assignmentId (MoveAssignmentCommand/Handler + IAssignmentRepository.move): one operation whose candidate roster excludes the source seat and appends the relocated one, replayed through the same FeasibilityGate, updating the existing row so the seat is never duplicated (H1 would reject it) nor lost (a mid-way failure would drop it). staffId is deliberately not accepted -- moving a seat to a different PERSON is a remove plus an add. Verified live: same 24/24h staff member, POST still 422s (correct, it is still an add), PATCH succeeds with the id preserved and source flipped to MANUAL, and H2/H3/H4 still reject what they should; then re-verified through the real browser UI by dispatching genuine DragEvents at the grid. Lesson: when two individually-correct endpoints are composed by the UI into one user-visible operation, the composition is a third operation with its own preconditions -- and if the domain has an aggregate constraint (a weekly cap is an aggregate over rows, unlike an overlap check), the intermediate state between the two calls is a state the domain forbids. Give the composed operation its own endpoint and validate the END state, never the intermediate one.", "context": "apps/web/src/components/roster-manager.tsx (handleDrop), apps/web/src/lib/api-client.ts (moveAssignment), apps/scheduler-api/src/modules/scheduling/application/commands/move-assignment/, domain/repositories/assignment.repository.ts, presentation/controllers/roster.controller.ts"}
{"timestamp": "2026-08-20T05:30:00.000Z", "type": "errors", "title": "`npm run dev` was broken on a FRESH CLONE for the whole project's life: turbo.json's `dev` was the one task missing dependsOn ^build, so apps/scheduler-api could not resolve @scheduler/shared-kernel or @scheduler/scheduling-core", "detail": "Found on the final pre-submission check by cloning the PUBLIC submission repo to a clean directory and running exactly the two commands RUN.md documents. `npm install && npm run setup` worked; `npm run dev` brought up apps/web but never apps/scheduler-api. Running the API alone showed the cause: ~30 TS2307 'Cannot find module @scheduler/shared-kernel' / '@scheduler/scheduling-core'. Both packages resolve through package.json main/types to ./dist/, which only their `build` script produces, and turbo.json's `dev` task declared only {cache:false, persistent:true} -- no dependsOn. `start` had dependsOn:[build], `typecheck` and `test` both had dependsOn:[^build]; `dev` alone did not. Invisible for the entire project because every local working tree had run a build at some point, so dist/ was always sitting there; and invisible to CI because CI runs typecheck/lint/test, every one of which builds its dependencies first. The user-visible symptom was the worst possible one for a take-home: a reviewer following the README gets a UI that only ever shows its 'the scheduling service didn't answer' boundary, and no obvious reason why. Fixed by adding dependsOn:[^build] to `dev`. Verified by deleting packages/*/dist, apps/*/dist, apps/web/.next and .turbo from the fresh clone and re-running `npm run dev`: turbo built both libraries first, the API came up, and the UI returned 200; then the whole flow was exercised against that clone's own virgin database (seed -> auto-schedule -> 38 assignments, 0 unfilled seats, 0 staff below the fair-share target). One incidental gotcha along the way: turbo.json's `//` key must be a STRING, not an array -- an array fails with turbo_json_parse_error and turbo refuses to run at all. Lesson: a persistent task needs its build dependencies declared just like a one-shot task does, and the only test that can catch a missing one is a fresh clone into a directory that has never been built in -- exactly the same class of failure as the .gitignore `coverage/` incident logged earlier, and caught the same way.", "context": "turbo.json (tasks.dev), packages/shared-kernel/package.json + packages/scheduling-core/package.json (main/types -> dist/), RUN.md, docs/09_running_it.md"}
