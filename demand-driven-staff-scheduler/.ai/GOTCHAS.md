# 🔥 Known Gotchas & Lessons Learned (from memory)

> **Auto-generated by `.ai/knowledge_builder.py`** — do not hand-edit; append to
> `.ai/memory/*.jsonl` instead (canonical entry shape: `directives/memory_sop.md`).
> Last updated: 2026-08-19T06:13:46.208405+00:00
>
> Real problems hit during development. **Search this BEFORE debugging** to avoid repeating them.
> Split out of `KNOWLEDGE_INDEX.md` on 2026-08-07: it was 63% of that file's tokens while being
> needed only for debugging, and the index is read at every session start.
> All 67 entries shown; bodies are clipped — full text in `.ai/memory/*.jsonl`.

- **UX pass driven by user screenshots: dropzone/heatmap proportion inverted, 112-row summary became a day×hour grid (which the brief explicitly permits), coverage…** `[apps/web/src/components/{demand-manager,summary-view,coverage-view,roster-manager,shift-manager,staff-manager}.tsx, components/ui/button.tsx]`
  → User reviewed the six screens from screenshots and gave concrete complaints rather than a vague 'make it nicer', so each one was fixed against a measurable target and verified with getComputedStyle/getBoundingClientRect rather than by eye. (1) Demand: the CSV dropzone was p-8 (1…
- **.gitignore's `coverage/` (meant for Jest/Vitest reports) silently swallowed apps/web/src/app/s/[id]/coverage/ — the Coverage SCREEN, a brief §8 stretch goal, w…** `[.gitignore (line 9 coverage/), apps/web/src/app/s/[id]/coverage/page.tsx]`
  → Caught on the final pre-submission pass, and only because that pass ran the app from a FRESH CLONE with `npm run start` (production build) instead of `npm run dev` in the working tree. Symptom chain: /s/:id/coverage returned 404 while the API returned 200 for the same schedule i…
- **Added a root `npm run start` to run the BUILT artifacts (node dist/main + next start) via Turbo — required repointing apps/scheduler-api's bare "start" script,…** `[package.json (root start script), turbo.json (start task), apps/scheduler-api/package.json (start repointed to node dist/main)]`
  → User noticed the root package.json only had `dev` and `build`, no way to run the production build. Both apps already had a real prod-serve script (apps/web's `start` = `next start`; apps/scheduler-api's `start:prod` = `node dist/main`) but under DIFFERENT names, which matters be…
- **npm audit: fixed the one reachable production vulnerability (@fastify/static, unused code path), deliberately left next/vitest majors unpatched with the exploi…** `[apps/scheduler-api/package.json (@fastify/static), package-lock.json. Deferred: next (15->16), vitest (2->4) and their transitive chains.]`
  → 9 findings (3 moderate, 5 high, 1 critical), and every single one required a semver-MAJOR bump -- `npm audit fix` (no --force) applied nothing and just listed 'breaking change' for all nine, confirming there was no free lunch here. Rather than running --force blindly right befor…
- **Visual design pass upgraded the 6 shared UI primitives + shell only, never the 4 large screen components — the highest-leverage, lowest-risk surface for a "mak…** `[apps/web/tailwind.config.ts, apps/web/src/app/{layout,page,globals.css}, apps/web/src/app/s/[id]/layout.tsx, apps/web/src/components/{ui/*.tsx,nav/schedule-tabs.tsx}]`
  → User asked to make the UI look better, with nothing else specified. Rather than touching staff-manager.tsx/roster-manager.tsx/shift-manager.tsx/demand-manager.tsx (2000+ combined lines of tested business logic), the change targeted only what every screen composes from: the 6 pri…
- **Coverage grid label read "Required staff vs. scheduled staff" while the cell rendered `${scheduled}/${required}` — the two were inverted, so a manager would re…** `[apps/web/src/components/coverage-view.tsx]`
  → Found during the final pre-submission UI pass, once port 3000 was free and the app could be driven in its SHIPPED configuration. coverage-view.tsx line 63 described the grid as 'Required staff vs. scheduled staff, per hour' but line 92 rendered `${cell.scheduled}/${cell.required…
- **Fresh-clone verification: turbo.json build.outputs listed only dist/** so apps/web (.next) was never cacheable, and a long scratchpad path produced a fake "Fil…** `[turbo.json (build.outputs)]`
  → Final pre-submission check ran the reviewer's actual path — git clone to an empty directory, then the five commands RUN.md documents. Two things surfaced. (1) A REAL config gap: turbo.json's build task declared outputs: ['dist/**'], which covers packages/* and apps/scheduler-api…
- **CASE_STUDY.md/.vi.md — 1000+ lines at the repo root, linked from nothing, and never once audited: still listed four SHIPPED stretch goals as "not built"** `[CASE_STUDY.md, CASE_STUDY.vi.md]`
  → Asked what was left to handle, a grep for stale test counts incidentally surfaced CASE_STUDY.md, a 511-line document (plus a 520-line Vietnamese twin) sitting at the repo root next to readme.md. It had never been checked in any prior audit because every audit had worked from a l…
- **Prove a subtle-branch test is load-bearing by mutating the branch, not by watching it pass** `[apps/scheduler-api/src/modules/scheduling/application/commands/add-assignment/add-assignment.handler.ts]`
  → AddAssignmentHandler only throws for a violation attributed to the CANDIDATE, deliberately ignoring violations owned by pre-existing assignments (a cap lowered after auto-schedule ran, say). A test asserted that, and passed — but passing proves nothing about whether the test wou…
- **A 218-test total hid that all 24 CQRS handlers had zero tests — the application layer was covered only by manual curl/browser runs** `[apps/scheduler-api/src/modules/scheduling/application/**/*.spec.ts, docs/08_testing_strategy.md]`
  → Asked whether tests were complete, the honest answer needed counting rather than trusting the headline number. apps/scheduler-api had 5 spec files: four infrastructure (Zod pipe, exception filter, logging interceptor, transient-error classification) plus the CSV parser — and not…
- **jest.Mocked<T> does not deep-mock nested objects: tx.assignments.replaceAll.mock.calls fails typecheck even though the runtime value is a jest.fn()** `[apps/scheduler-api/src/modules/scheduling/application/commands/auto-schedule/auto-schedule.handler.spec.ts, domain/entities/staff-unavailability.entity.ts]`
  → SchedulerApiRepos is an object OF repository objects, and the documented `as unknown as jest.Mocked<SchedulerApiRepos>` cast types the outer object only — every nested method keeps its real signature. So `expect(tx.assignments.replaceAll).toHaveBeenCalled()` compiles fine (jest…
- **roleShortfalls() initially fired on every day a shift existed, including closed days (floor=target=0) -- reported a false shortfall on Sunday for the real seed…** `[packages/scheduling-core/src/reporting/diagnostics.ts (roleShortfalls), packages/scheduling-core/src/golden.spec.ts, packages/scheduling-core/src/reporting/diagnostics.spec.ts]`
  → First implementation looped every day in ShiftRequirements x every shift with roleRequirements, flagging assigned < required with no floor/target check -- unlike unfilledSeats() and assigner.tss enumerateSeats(), both of which already skip a (day, shift) with floor===0 && target…
- **build-scheduling-input.ts refactored from 4 positional args to an options object -- convention going forward: a shared row-shaping function with more than ~3 p…** `[apps/scheduler-api/src/modules/scheduling/application/shared/build-scheduling-input.ts]`
  → buildSchedulingInput(schedule, staff, shifts, demandCells) was already at 4 positional args before the stretch goals; H4 needed a 5th (unavailability) and roles needed a 6th/7th (staffRoles, shiftRoleRequirements) -- widened to a single BuildSchedulingInputOptions object instead…
- **Role minimums (stretch goal) enforced as a seat-filling pass (rolePass), not a sixth FeasibilityGate constraint -- new ADR-0006** `[packages/scheduling-core/src/assignment/assigner.ts, packages/scheduling-core/src/assignment/rebalancer.ts, packages/scheduling-core/src/reporting/diagnostics.ts, docs/adr/0006-role-requirements-as-seat-requirements.md]`
  → The obvious first instinct given ADR-0001s H1-H4 precedent was a fifth/sixth gate case answering "may this candidate take this seat". Rejected: a role minimum answers a structurally different question ("is this seat legal yet, regardless of who"), and forcing it into Feasibility…
- **User correction: the backend architecture is baseline engineering, not scope surplus — README now states scope explicitly instead of apologising for the stack** `[readme.md (## Scope), readme.vi.md (## Phạm vi)]`
  → I had been framing the NestJS/CQRS/Postgres backend defensively, repeatedly hedging it against the brief's 'do not gold-plate' line (section 9) and treating the public-repo/link questions as risks. The user corrected both premises directly: the company authorised a public repo,…
- **Verifying the UI in a browser when another project holds the only CORS-allowed port: use a process-scoped env override, not a container stop or a committed-con…** `[apps/scheduler-api (CORS_ALLOWED_ORIGINS process env override), .claude/launch.json (temp web-3001 entry, reverted via git checkout)]`
  → Needed to verify apps/web in a real browser, but port 3000 — apps/web's configured port AND the only origin in CORS_ALLOWED_ORIGINS — was held by an unrelated scenario's scheduler-grafana container (via wslrelay). Three tempting-but-wrong options: stop the other project's contai…
- **The three graded DELIVERABLES (README, AI-usage note, assumptions doc) had drifted worse than the code ever did -- the AI-usage note described the stack revers…** `[docs/12_ai_collaboration.md, readme.md, readme.vi.md, docs/README.md, SWE_Take-Home_Staff_Scheduling_System.pdf]`
  → User asked for a careful re-check of whether the repo meets SWE_Take-Home_Staff_Scheduling_System.pdf. Read the PDF DIRECTLY this time instead of docs/01_business_requirements.md (the repo's own quote of it) -- which is what surfaced everything, because the derived doc omits the…
- **docs/01_business_requirements.md Assumption #15 still claimed SQLite/no-Docker after the backend architecture reversal moved persistence to PostgreSQL + Docker** `[docs/01_business_requirements.md (Assumption #15)]`
  → A user-requested audit of whether the take-home brief is fully met found Assumption #15 (Persistence: database or browser storage?) still described the PRE-reversal decision -- "SQLite via Prisma (file:./dev.db)...no database server, no Docker, no ports" -- verbatim, even though…
- **packages/scheduling-core's ESLint config was missing tsconfigRootDir, and its build tsconfig excluded spec files that the eslint file glob still matched -- two…** `[packages/scheduling-core/eslint.config.js, packages/scheduling-core/tsconfig.eslint.json, packages/scheduling-core/tsconfig.json]`
  → User's editor showed a red squiggly on index.ts (a file that lints clean on the CLI) with 'TSConfig does not include this file'. Root cause 1: packages/scheduling-core/eslint.config.js was the only one of the 4 workspace ESLint configs (apps/scheduler-api, packages/shared-kernel…
- **Bundled the always-together (gate, state) pair into one RosterContext value object, threaded through fairnessPass/coveragePass/rebalance/buildDiagnostics inste…** `[packages/scheduling-core/src/assignment/feasibility-gate.ts, packages/scheduling-core/src/assignment/assigner.ts, packages/scheduling-core/src/assignment/rebalancer.ts, packages/scheduling-core/src/reporting/diagnostics.ts, packages/scheduling-core/src/index.ts, apps/scheduler-api/src/modules/scheduling/application/queries/get-coverage/get-coverage.handler.ts]`
  → User asked to reconsider directives/domain_modeling.md S1 ("no second mutable class") because manually threading gate+state through every stage function felt error-prone -- proposed wrapping the pipeline in an OOP class with this.state. Clarified the directive only forbids a SEC…
- **index.ts export list now grouped with per-stage comments (Stage 0..5) and a header note naming the 4 exports the app actually calls** `[packages/scheduling-core/src/index.ts]`
  → User found index.ts hard to read: a flat list of 8 export lines with no indication of which stage each line belonged to, or which exports were load-bearing for apps/scheduler-api versus only exposed for the package's own tests/validateRoster replay. Comment-only change, no logic…
- **Split assignment/feasibility-gate.ts (155 lines) into eligibility-brand.ts + roster-state.ts + feasibility-gate.ts -- file organization only, still exactly 2 m…** `[packages/scheduling-core/src/assignment/feasibility-gate.ts, packages/scheduling-core/src/assignment/eligibility-brand.ts, packages/scheduling-core/src/assignment/roster-state.ts, directives/domain_modeling.md]`
  → User found the file hard to read: the Eligibility Symbol-branding trick, the RosterState class (7 methods), and the FeasibilityGate class (H1-H3) were all in one file, so reading a rule meant scrolling past unrelated branding-mechanics comments first. Considered converting sched…
- **GET /schedules/:id/suggested-n now 422s via a new InsufficientCalibrationDataError when staff, shifts, or demand cells are empty, instead of silently returning…** `[apps/scheduler-api/src/common/errors/scheduling.error.ts, apps/scheduler-api/src/modules/scheduling/application/queries/suggest-n/suggest-n.handler.ts]`
  → Traced the full call path (suggest-n.handler.ts -> buildSchedulingInput -> suggestTransactionsPerStaff in packages/scheduling-core) and confirmed every empty-input edge case (no staff, no shifts, no demand) was ALREADY crash-safe by construction -- computeRequiredStaff loops ove…
- **A Client Component's `useState(propValue)` does NOT re-sync when the parent Server Component re-renders with a new prop after router.refresh() -- only the FIRS…** `[apps/web/src/components/roster-manager.tsx]`
  → `RosterManager` seeded `const [currentSuggestedN, setCurrentSuggestedN] = useState(suggestedN)` from a server-fetched prop. After 'Save parameters' succeeded (PATCH 200, confirmed via network log) and called `router.refresh()`, the Server Component re-fetched a fresh `suggestedN…
- **api-client.ts request() sent Content-Type: application/json on a BODYLESS call (autoSchedule's POST, every DELETE) -> Fastify 400s with 'Body cannot be empty w…** `[apps/web/src/lib/api-client.ts request()]`
  → Fix: only set Content-Type when init.body !== undefined and is not FormData (`const hasBody = init?.body !== undefined`). Symptom: clicking Auto-schedule in the browser threw that exact string as a banner.
- **api-client.ts request() always parsed the response as JSON, including on 204 No Content (every removeX DELETE) -> the failed parse was read as body?.success be…** `[apps/web/src/lib/api-client.ts request()]`
  → Fix: short-circuit on `res.status === 204` before attempting res.json(), returning undefined without parsing. Symptom: clicking Remove on a staff/shift/roster-assignment row banner-errored 'Request failed with status 204' despite the DELETE having actually succeeded server-side.
- **@fastify/cors v11's own default `methods` is 'GET,HEAD,POST' -- narrower than the popular `cors` npm package's default (GET,HEAD,PUT,PATCH,POST,DELETE) -- sile…** `[apps/scheduler-api/src/bootstrap/fastify.ts (cors registration)]`
  → `fastify.register(cors, { origin, credentials })` never set `methods` explicitly, relying on the plugin's default -- assumed (wrongly, without checking) to match the `cors` npm package everyone is more familiar with. Confirmed via `curl -X OPTIONS ... -H 'Access-Control-Request-…
- **apps/web's mutation screens refresh via router.refresh() after every write, never local optimistic state -- one pattern across all six new screens (staff/deman…** `[apps/web/src/components/{staff,shift,demand,roster}-manager.tsx, summary-view.tsx, coverage-view.tsx]`
  → Every new screen's mutation handler (addStaff, updateShift, addAssignment, autoSchedule, ...) does: setPending(true) -> await the api-client call -> on success call next/navigation's router.refresh() (re-runs the Server Component's data fetch for the current route) -> on failure…
- **apps/web had NO .prettierrc of its own (unlike apps/scheduler-api and packages/shared-kernel) -- running a root-level `npx prettier --write` on it reformats to…** `[apps/web/.prettierrc (new file), apps/web/package.json (still has no format/format:check script)]`
  → apps/web's existing files (api-client.ts, create-schedule-form.tsx) were hand-written in single-quote/no-semicolon style matching apps/scheduler-api's/packages/shared-kernel's .prettierrc, but apps/web itself had no .prettierrc and no `format`/`format:check` script in package.js…
- **Three docstrings (shift.entity.ts, create-schedule.handler.ts, SchedulesController.create) claimed CreateScheduleHandler seeds the two default shifts -- it doe…** `[apps/scheduler-api/src/modules/scheduling/infrastructure/repositories/prisma-schedule.repository.ts (PrismaScheduleRepository.create), application/commands/create-schedule/create-schedule.handler.ts]`
  → Building the Phase 3 UI plan, I read CreateScheduleHandler's body (`return tx.schedules.create(...)`), saw no shift-seeding code, and concluded (matching three docstrings that agreed with each other) that schedules created through the API ship with 0 shifts -- a real bug, planne…
- **Cortex VI->EN translation COMPLETE: all 19 directives are now English-only, and Cortex readme is split into readme.md (EN) + readme.vi.md (VI). The lineage now…** `[../../distributed-social-platform/ (directives/ + readme.md + readme.vi.md)]`
  → Finished the work logged in the previous conventions entry (which recorded 10 of 16 files done). Translated in this pass: resilience_patterns.md (the largest, 844 lines total / 339 VI - and the LAST file shared with both scenarios), eventing_patterns.md, microservice_architectur…
- **Language rule set by the user: directives = English only (few readers); README/CASE_STUDY = bilingual EN+VI (many readers). Cortex is being translated VI→EN to…** `[../../distributed-social-platform/directives/ (10 of 16 files translated)]`
  → User's rule, stated directly: documents few people read (directives — agent/dev only) are English-only; documents many people read (README, CASE_STUDY — the public entry points) get both an English and a Vietnamese version. This resolves the lineage inconsistency found earlier:…
- **Audited Cortex (distributed-social-platform) as the upstream root of the shared directives — it is CLEAN; every documentation defect found this session was int…** `[../../distributed-social-platform/directives/ (read-only audit)]`
  → Checks run against ../../distributed-social-platform: (1) every `directives/*.md` path referenced anywhere in its directives/docs/.ai resolves to a real file — the single apparent miss, `event_driven_sop.md`, appears only in two plans as a PROPOSAL ('cân nhắc thêm' = consider ad…
- **The _templates/ folder I created to PREVENT referenced-but-missing docs was itself full of them — ~34 scenario-local citations (plan §N, ADR-000N, assumption N…** `[../_templates/directives/*.md]`
  → Built the templates by copying scenario 02's specialized directives, which carried every scenario-local citation with them: `.ai/plans/backend-architecture-reversal.plan.md §4/§6`, `docs/adr/0005-transaction-retry-boundary.md`, `ADR-0001`/`ADR-0004`, `plan §0.1/§2.1/§2.2/§4/§7/§…
- **FIVE common directives were missing from this repo, not one: cqrs_pattern, folder_structure_sop, logging_standard, database_standard, observability_monitoring…** `[directives/ (5 files ported), ../_templates/directives/]`
  → User corrected the model behind the earlier 'not ported, doesn't apply' calls: these are COMMON documents (Cortex -> service-appointment-scheduler -> here). Surplus is not wrong -- a scenario may not use a section, but it must not delete it, because a rule deleted for being unus…
- **../_templates/directives/ now holds all 13 common directives with a mandatory `<!-- TEMPLATE ... SPECIALIZE: ... -->` header, not just the 3 I first judged tem…** `[../_templates/README.md, ../_templates/directives/*.md]`
  → Earlier I argued only memory_sop/qa_standard/cqrs_pattern were templatable and that naming_conventions/domain_modeling/zod_validation/testing_standard were 'correctly scenario-specific'. That was wrong in the same direction as the missing-directives bug: those files' RULES are s…
- **Cross-scenario audit rule: process/convention directives must be diffable against the sibling scenario and differ ONLY in fill-in slots; business/domain docs m…** `[directives/*.md, ../_templates/directives/]`
  → User's rule, stated explicitly: documents that are business-specific may differ between scenarios, but shared process/coding-architecture/testing/convention documents must stay the same. Enforced this pass by literally diffing every shared file between service-appointment-schedu…
- **CLAUDE.md and AGENTS.md -- the files auto-loaded at EVERY session start -- still instructed 'Do NOT reintroduce NestJS, Fastify, PostgreSQL, Docker, Turborepo,…** `[CLAUDE.md, AGENTS.md]`
  → The backend-architecture reversal (phases A-F) rebuilt this repo on exactly the stack CLAUDE.md's Project Context forbids, and neither entry point was updated -- Phase F's doc reconciliation covered docs/03, docs/09, readme.md, RUN.md but never opened AGENTS.md/CLAUDE.md, becaus…
- **Created ../_templates/ at the collection root -- canonical copies of the 3 directives that are genuinely stack-agnostic in mechanism (memory_sop.md, qa_standar…** `[../_templates/ (collection root, outside any single scenario)]`
  → User's suggestion, in response to finding real drift (cqrs_pattern.md missing, qa_standard.md pointing at a deleted directory) between what should have been near-identical files across the two scenarios in this collection. ../_templates/directives/ holds {{PLACEHOLDER}}-marked v…
- **directives/cqrs_pattern.md was referenced by name in 8 real code/doc locations (scheduler-api-repos.ts, scheduling.query-repository.ts, every controller, eslin…** `[directives/cqrs_pattern.md (was missing entirely)]`
  → User asked to cross-check which directives should be near-identical across the two scenarios in this collection, since apps/scheduler-api's CQRS mechanism is packages/shared-kernel ported byte-for-byte from ../service-appointment-scheduler -- that mechanism's own directive (cqrs…
- **The last three stale directives (naming_conventions.md, domain_modeling.md, zod_validation.md) reconciled to the real NestJS/CQRS shape -- closes the Live-debt…** `[directives/naming_conventions.md, directives/domain_modeling.md, directives/zod_validation.md, directives/README.md]`
  → Rewritten against the actual code in apps/scheduler-api, not the plan's prose or the old Next.js shape. naming_conventions.md gained 4 new groups this repo genuinely has now (Repository I{Entity}Repository/Prisma{Entity}Repository, CQRS Command/Query Handler, domain error class…
- **CASE_STUDY.md/.vi.md written for scenario 02, plus readme.vi.md for BOTH scenario 02 and service-appointment-scheduler -- the collection's bilingual-entry-poin…** `[CASE_STUDY.md, CASE_STUDY.vi.md, readme.vi.md, ../README.md, ../README.vi.md, ../service-appointment-scheduler/readme.vi.md]`
  → User-directed scope (explicit AskUserQuestion confirmation): CASE_STUDY.md/.vi.md for this scenario, matching service-appointment-scheduler's seven-group A-G structure and depth (not a shorter version); readme.vi.md added to BOTH this scenario and ../service-appointment-schedule…
- **scripts/sync.cjs's own Prisma-generate trigger silently kept pointing at apps/web/prisma/ after Phase E deleted it -- the Stop hook would have run `npx prisma…** `[scripts/sync.cjs]`
  → Phase E deleted apps/web/prisma/ entirely (moved persistence to apps/scheduler-api). scripts/sync.cjs's own `touched('apps/web/prisma/')` trigger and its `--schema=apps/web/prisma/schema.prisma` command were never updated -- this file is TOOLING, not app code, so it wasn't touch…
- **docs/adr/README.md's ADR index and its closing 'nothing is ported verbatim' line went stale the moment ADR-0005 was ported in Phase B, and nobody had reconcile…** `[docs/adr/README.md, docs/adr/0005-transaction-retry-boundary.md]`
  → ADR-0005 (transaction-retry-boundary, ported from ../service-appointment-scheduler in Phase B) was added to docs/adr/ as a file but never added to docs/adr/README.md's index table -- and that file's own closing sentence ('Numbering starts at 0001 because nothing is ported verbat…
- **Root .env was gitignored (never force-added) despite apps/web/.env being force-added for the same "zero setup" reason -- docs/09_running_it.md and RUN.md would…** `[.env, .gitignore, apps/web/.env (the existing precedent)]`
  → The root .gitignore has a bare `.env` line, which git matches at every directory level -- apps/web/.env is tracked anyway because it was explicitly `git add -f`'d early on (its own comment: 'not a secret, a local path, committed deliberately for zero setup'). Root .env, read by…
- **Dropped @fastify/static from apps/scheduler-api's dependencies while porting -- @nestjs/platform-fastify needs it present even when useStaticAssets() is never…** `[apps/scheduler-api/package.json]`
  → package.json ported from ../service-appointment-scheduler had @fastify/static listed; it looked unused (no useStaticAssets() call anywhere in this app) so it was left out when trimming dependencies. NestFactory.create with FastifyAdapter throws at boot: 'The "@fastify/static" pa…
- **packages/scheduling-core's package.json pointed main/types straight at TS source -- worked under Vitest, crashed under a real Node require()** `[packages/scheduling-core/package.json, tsconfig.json]`
  → package.json had "main": "./src/index.ts" (never built). vitest transpiles TS on the fly so all 80 scheduling-core specs and apps/web's earlier Next.js usage never surfaced the gap. The moment apps/scheduler-api's compiled CommonJS dist/main.js did `require('@scheduler/schedulin…
- **apps/web (SQLite schema) and apps/scheduler-api (PostgreSQL schema) shared ONE generated Prisma client output directory -- whichever ran `prisma generate` last…** `[apps/scheduler-api/prisma/schema.prisma, apps/web/prisma/schema.prisma (now deleted)]`
  → Both apps generate into the workspace-hoisted root node_modules/@prisma/client (Prisma's default client output location, never overridden per-app). apps/scheduler-api crashed at boot ("Error validating datasource `db`: the URL must start with the protocol `file:`", errorCode P10…
- **tsconfig.base.json's lib: ["ES2022"] has no DOM types -- fine for the Node-shaped backend packages, but apps/web never had client-side code (an onChange handle…** `[apps/web/tsconfig.json]`
  → `e.target.value` on an <input>'s onChange handler type-errored as 'Property value does not exist on type EventTarget & HTMLInputElement' -- correct symptom for a missing DOM lib despite HTMLInputElement genuinely having .value, because without "DOM" in `lib`, TS's HTMLInputEleme…
- **Coverage view recomputes Diagnostics LIVE from the persisted roster -- docs/04_data_model.md originally specified the opposite (read ScheduleRun.diagnostics),…** `[apps/scheduler-api/src/modules/scheduling/application/queries/get-coverage/get-coverage.handler.ts]`
  → docs/04_data_model.md said the coverage view should read ScheduleRun.diagnostics (a stored snapshot from the last auto-schedule run) specifically to avoid recomputing something that might disagree with what the manager saw at generation time. That reasoning predates manual roste…
- **buildSchedulingInput extracted to application/shared/ -- both AutoScheduleHandler and AddAssignmentHandler must shape Prisma rows into SchedulingInput identica…** `[apps/scheduler-api/src/modules/scheduling/application/shared/build-scheduling-input.ts]`
  → generateRoster (auto-schedule) and validateRoster (manual roster edit) both need the exact same SchedulingInput built from the same Schedule/StaffMember/Shift/DemandCell rows -- scheduling-core's own index.ts docstring warns about this class of drift for the FeasibilityGate itse…
- **A stale generated @prisma/client (from an earlier session/branch) type-errors on fields that ARE in schema.prisma -- 'does not exist in type ...UpdateInput', n…** `[apps/scheduler-api (Prisma-generated client)]`
  → Mid-session typecheck of apps/scheduler-api reported deletedAt/parameters/diagnostics as unknown properties on Shift/StaffMember/ScheduleRun update inputs, even though schema.prisma has always declared them. The generated client under node_modules/@prisma/client had gone stale r…
- **ADR numbering collision when porting shared-kernel: source's ADR-0001 (transaction/CQRS boundary) had to become ADR-0005 here** `[docs/adr/0005-transaction-retry-boundary.md; packages/shared-kernel/src/**]`
  → packages/shared-kernel code (command-bus.ts, tx-scope.ts, cqrs.error.ts, several interfaces) cites its governing ADR by number AND section (ADR-0001 SS2.3/SS2.4 etc.) in the source repo. This repo's ADR-0001-0004 are already taken by the scheduling-algorithm ADRs (constraint enf…
- **service-appointment-scheduler's shared-kernel AppError had a stray `readonly?: string` property, unrelated to the intended `readonly details?` line** `[packages/shared-kernel/src/errors/app-error.ts]`
  → packages/shared-kernel/src/errors/app-error.ts in the source repo declares `readonly?: string` immediately above `readonly details?: ErrorDetails` -- an unused, likely-accidental property (probably a half-finished edit of the details line) that adds a meaningless optional `reado…
- **D1 resolved by the user: ship N=18 as the default, let suggestTransactionsPerStaff honestly return 15** `[.ai/plans/phase-1-algorithm.plan.md §1 D1 and §2.0; docs/adr/0003-demand-to-headcount-model.md; apps/web/prisma/schema.prisma]`
  → Chosen against two alternatives that were put on the table with their costs: (B) move the default to 15 - rejected because a new migration plus a re-derived §7.8 table would drop the demo's slack from 8 seats to 2 (32<37<44<46), a knife-edge outcome that could flip on an impleme…
- **A branded unique-symbol type with the brand alongside top-level fields does NOT block a same-shape `as X` cast** `[packages/scheduling-core/src/assignment/feasibility-gate.ts]`
  → First FeasibilityGate.Eligibility design put staffId/day/shift at the top level next to the `[ELIGIBILITY_BRAND]` symbol property. `({staffId, day, shift} as Eligibility)` (no `unknown` bridge) compiled without error — caught by the `@ts-expect-error` test itself reporting 'Unus…
- **An executed plan is annotated in the successor plan, never edited in place** `[CLAUDE.md Citation Protocol; .ai/plans/init-source.plan.md §9; apps/web/.env.example; docs/09_running_it.md]`
  → Phase 0's agent proposed 'sua lai cau do trong plan §9/assumption 15' after finding that plan §9's 'no .env required (SQLite path has a default)' is technically false - Prisma's env("DATABASE_URL") has no default syntax, so a missing .env fails prisma generate with P1012. The di…
- **init plan §7.2's calibration rule does not produce its own stated answer of N=18 — it produces N=15** `[.ai/plans/init-source.plan.md §7.2, §7.8; .ai/plans/phase-1-algorithm.plan.md §1 D1; apps/web/prisma/schema.prisma]`
  → Plan §7.2 specifies suggestTransactionsPerStaff as a binary search for N where floorStaffHours(N) ~= targetUtilisation (0.8) x sum(maxWeeklyHours) (368) = 294.4, then asserts this 'yields N ~= 18 - 272 floor staff-hours against a 294-hour target'. Re-deriving floorStaffHours ove…
- **A partial PATCH on Shift needs a merged-state guard beyond Zod -- InvalidShiftTimeRangeError, not a second validator** `[apps/scheduler-api/src/modules/scheduling/application/commands/update-shift/update-shift.handler.ts]`
  → createShiftSchema/updateShiftSchema's .refine(endMinute > startMinute) only sees the PATCH BODY, not the existing row -- a request patching only { endMinute: 100 } against an existing { startMinute: 600, endMinute: 900 } passes Zod's refine trivially (startMinute is undefined in…
- **apps/scheduler-api jest config was silently unable to load @scheduler/shared-kernel in any spec that imports it -- missing the .js-extension-strip moduleNameMa…** `[apps/scheduler-api/package.json jest.moduleNameMapper]`
  → shared-kernel's source uses NodeNext-style relative imports (./errors/app-error.js) so its OWN package.json jest config carries a moduleNameMapper rule { "^(\\.{1,2}/.*)\\.js$": "$1" } to strip the extension before ts-jest's classic "node" moduleResolution looks the file up. app…
- **@fastify/multipart's declare module 'fastify' augmentation (adds .file()/.parts() to FastifyRequest) does not merge into apps/scheduler-api's own FastifyReques…** `[apps/scheduler-api/src/modules/scheduling/presentation/controllers/demand.controller.ts]`
  → @fastify/multipart hoists to the workspace ROOT node_modules, but 'fastify' itself is a nested, non-hoisted copy under apps/scheduler-api/node_modules (Phase C's @nestjs/platform-fastify version-pin fix, see this file's earlier fastify-duplication entry). TypeScript module augme…
- **Plan §10's literal grep checklist over-fires on legitimate contrastive prose; interpreted by intent, not letter** `[.ai/plans/init-source.plan.md §10]`
  → The checklist items `grep -ri "nestjs|fastify|postgres|docker|turbo"` (expect 0 outside plan §0.0) and `grep -ri "appointment|dealership|technician" apps/ packages/ docs/ directives/` (expect 0) both fire on docs/directives that legitimately explain what was rejected and why (e.…
- **sync.cjs's touched() check can't see files inside a wholly-new, never-staged directory** `[scripts/sync.cjs]`
  → `git status --short --porcelain` collapses an entirely untracked directory to one line (`?? packages/`) instead of listing files inside it, until at least one `git add` has run. scripts/sync.cjs's touched('packages/scheduling-core/src') substring check — ported unchanged from se…
- **apps/web's eslint.config.mjs (flat config) silently ignored all files without an explicit `files` glob** `[apps/web/eslint.config.mjs]`
  → ESLint 9 flat config with a plugin/rules block but no `files` key errored 'you are linting "src", but all files matching the glob pattern are ignored' — flat config has no implicit default file-matching the way .eslintrc did. Fixed by adding `files: ['**/*.{js,jsx,ts,tsx}']`. On…
- **postinstall db:setup fails on a fresh clone with no migrations yet — prisma migrate deploy applies nothing, doesn't create tables** `[apps/web/prisma/migrations/20260817053947_init]`
  → `prisma migrate deploy` only applies existing migration files under prisma/migrations/ — it does not create an initial migration from schema.prisma the way `migrate dev` does. Running db:setup before any migration existed silently reported 'No pending migrations to apply' and th…
- **Prisma's env("DATABASE_URL") has no default — a .env.example alone breaks the "no .env required" promise** `[apps/web/prisma/schema.prisma, apps/web/.env, .gitignore]`
  → schema.prisma used url = env("DATABASE_URL") with only apps/web/.env.example committed (real .env gitignored). A fresh `npm install` failed at postinstall with P1012 'Environment variable not found: DATABASE_URL'. Prisma cannot express a default value inline in the schema for an…
- **Phase 0 scaffold built directly from init-source.plan.md, no code before the plan** `[plan §12 Phase 0]`
  → Executed plan §12 Phase 0 in full: root workspace, packages/scheduling-core's skeleton (frozen public surface as throwing stubs, value objects implemented and tested), apps/web's Next.js shell + Prisma schema + seed, the ported/trimmed AI-workflow apparatus (directives rewritten…
