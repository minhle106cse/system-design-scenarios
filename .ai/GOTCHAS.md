# 🔥 Known Gotchas & Lessons Learned (from memory)

> **Auto-generated by `.ai/knowledge_builder.py`** — do not hand-edit; append to
> `.ai/memory/*.jsonl` instead (canonical entry shape: `directives/memory_sop.md`).
> Last updated: 2026-08-11T07:27:16.991613+00:00
>
> Real problems hit during development. **Search this BEFORE debugging** to avoid repeating them.
> Split out of `KNOWLEDGE_INDEX.md` on 2026-08-07: it was 63% of that file's tokens while being
> needed only for debugging, and the index is read at every session start.
> All 24 entries shown; bodies are clipped — full text in `.ai/memory/*.jsonl`.

- **Upgraded the CLAUDE.md/AGENTS.md drift check from warn-only to blocking, on request** `[scripts/sync.cjs blockReasons/blockOnceGuard/checkClaudeAgentsDrift, AGENTS.md, CLAUDE.md, .gitignore]`
  → The check landed warn-only (systemMessage, to the user's terminal) because upstream Cortex shipped it that way. User asked to make it block. A warning here is easy to read past mid-task -- the same failure mode the After-Task check itself existed to fix -- so a rule only a human…
- **sync.cjs --check wrote its own JSON and silently discarded every warning and the After-Task block** `[scripts/sync.cjs DRY_RUN branch]`
  → The DRY_RUN branch did `process.stdout.write(JSON.stringify({systemMessage: 'would run: ...'}))` and exited, bypassing emit(). So `npm run sync -- --check` — the invocation a human runs deliberately to see what sync would do — was the one path that showed neither the worktree/dr…
- **CLAUDE.md duplicates AGENTS.md's decision sections in full, because only CLAUDE.md is auto-loaded** `[CLAUDE.md, AGENTS.md, scripts/sync.cjs checkClaudeAgentsDrift]`
  → Claude Code injects CLAUDE.md's contents into the session's opening context and never AGENTS.md's — confirmed first-hand in this repo, not inferred. CLAUDE.md was a thin pointer saying 'read AGENTS.md for the Session Start Protocol, the After-Task Protocol, ...', which means the…
- **A fresh-clone check can 'pass' without checking anything out — Windows MAX_PATH plus a warm turbo cache** `[docs/09_devops_infrastructure.md, .ai/plans/submission-readiness.plan.md T1.2]`
  → Two independent traps hit the same verification step. (1) `git clone` into a deep temp path failed with `fatal: cannot create directory at 'apps/scheduler-api/prisma/migrations/20260810150000_service_type_duration_positive': Filename too long` followed by `warning: Clone succeed…
- **A doc abbreviating a DB predicate produced a false 'latent bug' finding — check pg_constraint, not the prose** `[.ai/plans/submission-readiness.plan.md F7, docs/02, docs/03, docs/06]`
  → An audit flagged that ADR-0002's exclusion constraints are scoped to `status = 'SCHEDULED'` and therefore ignore `deletedAt`, meaning a soft-deleted SCHEDULED appointment would strand its bay forever while every application read filtered it out. Wrong: `pg_get_constraintdef` sho…
- **An un-awaited write inside RxJS `tap` is invisible to unit tests and to manual cURL — only a real retry finds it** `[apps/scheduler-api/src/infrastructure/http/idempotency/idempotency.interceptor.ts, booking.e2e-spec.ts, docs/08]`
  → IdempotencyInterceptor persisted the handler's response with `tap(r => prisma.update(...).catch(log))`. `tap` does not wait, so the response reached the client while the idempotency row still held `response: null`. A client retrying promptly then read that null and got `409 alre…
- **Hardening pass after a green domain phase: audit finished work adversarially, reproduce every finding, then scope the fix** `[.ai/plans/hardening.plan.md, docs/12_ai_collaboration.md]`
  → The scheduler domain shipped with every gate green (92 tests, 3 endpoints demonstrably working). A deliberate adversarial pass against that finished work found 13 real problems, including 3 runtime defects no test caught: a mistyped customer/vehicle/dealership id returning 500;…
- **Clock-dependent specs with near-future fixture dates are time bombs — green today, red next month** `[apps/scheduler-api/src/modules/booking/application/queries/check-availability/check-availability.handler.spec.ts, domain/services/business-hours.ts]`
  → Adding a past-slot filter to CheckAvailabilityHandler made its specs depend on the real clock. The existing fixture date (2026-08-15, five days out at the time) would have started failing within a week — and it was ALSO a Saturday, which the new BUSINESS_DAYS default (Mon-Fri) t…
- **A metric incremented inside a transactional handler over-counts on COMMIT failure and again on retry** `[apps/scheduler-api/src/modules/booking/application/commands/book-appointment/book-appointment.handler.ts, directives/observability_monitoring.md]`
  → recordBookingAttempt('booked') fired at the end of BookAppointmentHandler.execute() — still inside the interactive transaction PrismaTxRunner opens. If COMMIT then failed, the counter recorded an appointment that never existed; and because CommandBus retries P2034, the retried a…
- **z.string().datetime() defaults to offset:false and rejects +07:00 — use z.iso.datetime({ offset: true })** `[apps/scheduler-api/src/modules/booking/presentation/schemas/book-appointment.schema.ts]`
  → Zod v4's z.string().datetime() accepts '2026-08-17T10:00:00Z' but REJECTS '2026-08-17T17:00:00+07:00' — the identical instant — because the default is offset:false. Since BUSINESS_TIMEZONE is configurable and the whole availability model is described in local hours, a client in…
- **A zero-duration ServiceType makes tstzrange EMPTY, silently disabling BOTH anti-double-booking exclusion constraints** `[apps/scheduler-api/prisma/migrations/20260810150000_service_type_duration_positive/migration.sql]`
  → endAt is derived as startAt + durationMinutes. With durationMinutes = 0, endAt == startAt and tstzrange(x, x, '[)') is the empty range, which overlaps NOTHING — so appointments_service_bay_no_overlap and appointments_technician_no_overlap both stop applying for that service type…
- **Prisma's soft-delete client extension filters find*/count only — a nested connect still resolves against a soft-deleted row** `[apps/scheduler-api/src/infrastructure/database/prisma/prisma.service.ts, modules/booking/application/commands/book-appointment/book-appointment.handler.ts]`
  → prisma.service.ts's extension gates on ['findUnique','findFirst','findMany','count'], so create({data:{customer:{connect:{id}}}}) is NOT filtered. Consequence found during the hardening audit: a booking could be created against a soft-deleted customer/vehicle/dealership with eve…
- **Reframed repo from a single Keyloop-assessment submission into Scenario 01 of a general system-design-scenarios collection** `[repo-wide rebrand + physical move to system-design-scenarios/service-appointment-scheduler]`
  → The owner wraps this repo inside a new parent folder (system-design-scenarios/), renamed this repo from keyloop-service-scheduler to service-appointment-scheduler, and stripped 'Keyloop Technical Assessment'/'this submission'/'the challenge' wording from readme.md, AGENTS.md, CL…
- **knowledge_builder.py's Project Overview block was ported as-is (per init plan) but hardcodes Cortex's product description as a Python string literal -- 'as-is'…** `[.ai/knowledge_builder.py lines ~285-314]`
  → init-source.plan.md section 6.2 said knowledge_builder.py is 'Copy as-is. Paths are all derived from WORKSPACE_ROOT' -- true for the file-discovery logic, but the script also has a hardcoded project_name/Product/Architecture/Database/Cache/Message-Broker/Search/AI/Frontend block…
- **`npm run check` was never run standalone during init — only its 3 sub-tasks separately — so a real Prettier failure survived until the fresh-clone test caught…** `[packages/shared-kernel/src/cqrs/command-bus.spec.ts, event-bus.spec.ts, packages/shared-kernel/src/logger/redact.spec.ts]`
  → Every verification pass during this init ran `npx turbo build/typecheck/lint/test` explicitly, but `npm run check` (= `turbo run typecheck lint format:check`) was never invoked as its own command — its third task, `format:check`, was never run at all. It turned out 3 shared-kern…
- **ADR-0003: availability computed as 3 transaction-scoped reads + in-memory selection, business hours from env config, slot conflicts never auto-retried** `[docs/adr/0003-availability-and-selection-policy.md, apps/scheduler-api/src/modules/booking/**]`
  → Neither docs/02_use_cases.md nor docs/06_api_contracts.md ever specified HOW availability is computed, who selects the bay/technician, where the GET /availability slot grid comes from, or whether a 23P01-derived conflict should be retried — ADR-0002 §6 explicitly deferred the la…
- **testing_standard.md's own `jest.Mocked<T> as unknown as jest.Mocked<T>` mocking pattern fails scheduler-api's own lint config, because repository interfaces us…** `[apps/scheduler-api/eslint.config.mjs, directives/testing_standard.md §2]`
  → Followed testing_standard.md §2 exactly: `mockRepo = { save: jest.fn(), ... } as unknown as jest.Mocked<IAppointmentRepository>`. Two type-aware eslint rules from tseslint.configs.recommendedTypeChecked fired on this exact, directive-sanctioned pattern: (1) @typescript-eslint/no…
- **A JSDoc comment containing the literal glob pattern modules/*/application/** self-terminates -- TypeScript reads */ as the comment's end** `[apps/scheduler-api/src/infrastructure/cqrs/scheduler-api-repos.ts, apps/scheduler-api/src/infrastructure/database/prisma/scheduler-api-repos.factory.ts]`
  → Wrote /** ... blocks that path from modules/*/application/** ... */ inside a JSDoc block, intending it as a quoted glob string. TS's tokenizer doesn't know it's 'inside backticks' semantically -- it just scans for the first */ after /**, which is the */ inside application/**. Ev…
- **Postgres 23P01 (exclusion violation) surfaces through @prisma/adapter-pg as P2039, not P2010, with the real error nested at error.meta.driverAdapterError.cause** `[apps/scheduler-api/src/modules/booking/infrastructure/repositories/exclusion-violation.ts, docs/adr/0003-availability-and-selection-policy.md §2.5]`
  → ADR-0002 said a conflicting insert 'surfaces as a Postgres error (23P01)' but never verified the exact shape Prisma wraps it in. Assumed P2010 (raw query failed) by analogy with older Prisma driver behavior. Provoked the real violation against live Postgres via a throwaway scrip…
- **`eslint-disable-next-line` only disables the literal next line — a multi-line wrapped comment breaks the target** `[apps/scheduler-api/src/infrastructure/database/prisma/scheduler-api-repos.factory.ts]`
  → Wrote a 3-line explanatory eslint-disable comment like `// eslint-disable-next-line RULE -- reason\n// continued reason\n// more reason\nexport interface Foo {}` expecting the disable to reach the interface declaration. It doesn't — `eslint-disable-next-line` disables violations…
- **@nestjs/platform-fastify pins an EXACT fastify version internally, duplicating the package and breaking Fastify plugin types** `[apps/scheduler-api/package.json, apps/scheduler-api/src/bootstrap/fastify.ts]`
  → apps/scheduler-api declared "fastify": "^5.8.5" (same range as Cortex). npm resolved that to 5.11.3 at the workspace root, but @nestjs/platform-fastify's own package.json pins "fastify": "5.10.0" as an exact dependency (not a range) — npm couldn't dedupe, so a second nested fast…
- **Prisma 7 schema.prisma no longer accepts `url` inside the datasource block** `[apps/scheduler-api/prisma/schema.prisma]`
  → Copied Cortex's schema.prisma pattern (datasource db { provider = "postgresql" }, no url line) but then added a url = env("SCHEDULER_DATABASE_URL") line out of habit from older Prisma versions — `prisma generate`/`migrate` immediately fails with P1012: 'The datasource property `…
- **Same hoisting-luck bug, second instance: @fastify/static missing crashes Swagger at boot, not at lint/typecheck/build** `[apps/scheduler-api/src/bootstrap/swagger.ts]`
  → SwaggerModule.setup() calls FastifyAdapter's useStaticAssets() internally to serve swagger-ui assets, which needs the optional peer @fastify/static. Cortex's core-api never lists it as a direct dependency either — it's a transitive dependency of something else in the Cortex mono…
- **Cortex's apps/core-api never declares @nestjs/cli, yet `nest build` worked there — pure hoisting luck** `[apps/scheduler-api/package.json]`
  → core-api/package.json has no @nestjs/cli devDependency at all. It only worked in Cortex because npm workspaces hoist devDependencies to the root node_modules/.bin, and 3 OTHER Cortex services (notification/search/worker-service) declare @nestjs/cli themselves, so core-api rode a…
